# Adaptive Bregman Proximal Stochastic Gradient with a Stabilized Barzilai–Borwein Step Size

Chenhan Jin<sup>1</sup> Shengze Xu<sup>1</sup> Binghui Xie<sup>1</sup> Kaiwen Zhou<sup>1</sup> Fan JIA<sup>2</sup> James Cheng<sup>1</sup> Tieyong Zeng<sup>3,4∗</sup>

<sup>1</sup>The Chinese University of Hong Kong <sup>2</sup>University of Utah <sup>3</sup>Beijing Normal-Hong Kong Baptist University <sup>4</sup>Guangzhou Nanfang College

## Abstract

Bregman proximal stochastic gradient (BPSG) methods bring variance-reduced composite optimization to objectives whose geometry is poorly captured by Euclidean smoothness. Their performance, however, remains sensitive to the step size: raw stochastic curvature estimates can fluctuate sharply, whereas line searches add repeated proximal evaluations. We introduce Ada-BPSG, a line-search-free BPSG method that couples the SAGA gradient table with a stabilized Barzilai–Borwein (BB) candidate. A mediant aggregates incremental secant information so that nearly singular local ratios receive little weight, and an explicit safeguard translates the resulting curvature estimate into the bounded step-size sequence required for convergence. This design yields a direct analytical chain from relative smoothness and component-wise variance control to convergence in finite-dimensional normed spaces. We prove an $O ( n / K )$ ergodic rate for convex objectives, a restarted linear rate under relative quadratic growth, and an $O ( 1 / K )$ bound for a Bregman proximal residual in the nonconvex setting. On logistic regression and sparse nonnegative matrix factorization, Ada-BPSG combines low objective values with substantially less sensitivity to the initial step size than standard variance-reduced baselines, while avoiding line search.

## 1 Introduction

We study finite-sum composite optimization on a finite-dimensional normed vector space E:

$$
\operatorname* { m i n } _ { x \in \mathbf { E } } F ( x ) : = f ( x ) + h ( x ) , \qquad f ( x ) : = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } f _ { i } ( x ) ,\tag{1.1}
$$

where $[ n ] : = \{ 1 , \dots , n \}$ indexes the component losses $f _ { i } : { \bf { E } }  \mathbb { R }$ , and $h : \mathbf { E }  ( - \infty , + \infty ]$ is a proper convex regularizer whose proximal subproblem is tractable. This model includes regularized empirical risk minimization, constrained learning, and structured estimation. When $h = 0$ and $\mathbf { E } = \mathbb { R } ^ { d }$ , stochastic gradient descent (SGD) samples $i _ { k } \in [ n ]$ and updates $x _ { k + 1 } =$ $x _ { k } - \eta _ { k } \nabla f _ { i _ { k } } ( x _ { k } )$ with step size $\eta _ { k } > 0$

For a nonsmooth h, the Euclidean proximal stochastic-gradient update replaces the gradient step by

$$
x _ { k + 1 } = \arg \operatorname* { m i n } _ { x } \left\{ \langle \widetilde { \nabla } _ { k } , x \rangle + \frac { 1 } { 2 \eta _ { k } } \| x - x _ { k } \| _ { 2 } ^ { 2 } + h ( x ) \right\} ,\tag{1.2}
$$

where $\widetilde { \nabla } _ { k }$ is an unbiased estimator of the full gradient $\nabla f ( x _ { k } )$ conditional on the iteration history. With a constant step size, the persistent variance of a plain sampled gradient can prevent the iterates from settling near a solution. Two complementary responses are therefore central to stochastic optimization: variance-reduced (VR) estimators, which make the gradient noise vanish, and adaptive step sizes, which respond to the local scale of the objective. The first family includes SAG, SAGA, SVRG, and SARAH [15, 23, 33, 36]; the second includes the Barzilai–Borwein (BB) rule, Polyak step sizes, AdaGrad, and Adam [5, 17, 26, 35]. Their combination has produced adaptive VR methods such as SVRG-BB, SAG-BB, AdaSVRG, AI-SARAH, ADASPIDER, AdProxGD, and 2AdaSPIDER [16, 21, 25, 31, 37, 40].

The Euclidean model behind (1.2) can nevertheless be a poor match for modern objectives. A globally Lipschitz Euclidean gradient is unavailable in applications such as neural-network training, quantum tomography, and matrix or tensor factorization [14, 20, 27]. Bregman geometry replaces the squared Euclidean distance by a divergence generated by a problem-adapted kernel ψ:

$$
x _ { k + 1 } = \arg \operatorname* { m i n } _ { x } \left\{ \langle \widetilde { \nabla } _ { k } , x \rangle + \frac { 1 } { \eta _ { k } } D _ { \psi } ( x , x _ { k } ) + h ( x ) \right\} .\tag{1.3}
$$

Section 3.1 formally defines the Bregman divergence $D _ { \psi }$ . The kernel encodes the geometry of the domain, while relative smoothness replaces a global Euclidean Lipschitz-gradient condition [7, 8, 29].

This geometric extension sharpens the step-size problem. Bregman divergences are generally asymmetric, so Euclidean polarization identities no longer drive the descent proof, and stochastic secant ratios may vary markedly across sampled components. Diminishing schedules trade this instability for slow progress, while line searches add repeated objective or proximal evaluations. Our goal is to retain stochastic curvature adaptation without either cost.

## 1.1 Related work and positioning

Bregman projection, forward–backward, mirror-prox, accelerated Bregman proximal-gradient, and relative-smooth first-order methods establish the deterministic geometric foundation of this work [6–9, 11, 19, 29, 41]. In Euclidean geometry, optimized proximal-gradient schemes have also been applied to structured inverse problems, including $\ell _ { 1 ^ { - } }$ and total-variation-regularized non-blind image deblurring [44]. Their stochastic counterparts incorporate sampled gradients, primal–dual splitting, or variance reduction [34, 38, 42, 43]. In particular, BPSG-SAGA and BPSG-SARAH extend Bregman proximal optimization to variance-reduced estimators, and extrapolated BPSG uses line search to control the resulting updates [42, 43].

Adaptive stochastic methods approach the same stability question through the step size. AdaGrad and Adam accumulate gradient statistics [17, 26, 45]; universal and relative-smoothness schemes estimate local regularity in mirror-descent, primal–dual, or extragradient frameworks [1–4, 13, 24, 28]; and BB methods extract scalar curvature from secant pairs [5]. Stabilized BB and BB-VR variants improve this estimate in stochastic Euclidean problems [10, 30, 40, 46, 47]. Related powered and double-adaptive methods use BB or SPIDER-style estimators in Euclidean finite-sum and minimax settings [21, 22, 46].

Ada-BPSG connects these lines through one mechanism: the SAGA table supplies incremental secant pairs, a stabilized BB rule converts them into a curvature-sensitive candidate, and clipping converts that candidate into a provably stable Bregman proximal step. Table 1 places it among the closest Bregman proximal methods. This leads to the central question of the paper:

Can a line-search-free BPSG method adapt to stochastic curvature while preserving convergence in non-Euclidean composite optimization?

## 1.2 Contributions

We answer this question with Ada-BPSG. Its contributions are threefold.

• A stable adaptive step from SAGA information.

Table 1: Positioning of Ada-BPSG among Bregman proximal (variance-reduced) methods. All operate on a distance-generating kernel ψ. “LS-free” marks methods needing no line search; “Needs $\bar { L } , M ^ { \prime \prime }$ lists the smoothness constants required to set the step or safeguard. Rates are ergodic orders in the iteration count K at fixed batch size; “lin. $( \mathrm { R Q G } ) ^ { \because }$ is the restarted linear rate under relative quadratic growth. A dash marks a regime the cited analysis does not cover: the stochastic Bregman baselines [42, 43] are established only in the nonconvex setting, so the convex ergodic and restarted-linear guarantees are, to our knowledge, new to Ada-BPSG. Crucially, our analysis is carried out in a general finite-dimensional normed space—using only the dual norm, with no Euclidean polarization or norm decomposition—so the matching $O ( n / K )$ order is obtained under strictly weaker geometric assumptions rather than by improving the rate. Over BPSG-SAGA, Ada-BPSG adds only two scalar accumulations per refreshed component.
<table><tr><td>Method</td><td>Oracle</td><td>Step-size rule</td><td>LS-free</td><td>Needs  ${ \bar { L } } , M$ </td><td>Convex</td><td>Nonconvex</td></tr><tr><td>BPG [8]</td><td>full</td><td>fixed 1/L</td><td>Yes</td><td>L</td><td> $O ( 1 / K )$ </td><td>O(1/K)</td></tr><tr><td>BPSG-SAGA [42, 43]</td><td>SAGA</td><td>fixed</td><td>Yes</td><td>L, M</td><td></td><td>O(1/K)</td></tr><tr><td>BPSGE [43]</td><td>SAGA/SARAH</td><td>line search</td><td>No</td><td>L, M</td><td></td><td>O(1/K)</td></tr><tr><td>Ada-BPSG (ours)</td><td>SÁGA</td><td>safeguarded BB</td><td>Yes</td><td>L, M</td><td> $O ( n / K )$  lin. (RQG)</td><td>O(1/K)</td></tr></table>

We aggregate recent component-wise secant pairs by a mediant, equivalently a denominatorweighted mean of local BB ratios. The weighting suppresses ratios associated with nearly singular curvature estimates. Clipping and a monotone update then turn this raw candidate into a bounded, line-search-free step-size sequence. For linear-prediction losses, the same quantities can be maintained with an $O ( n )$ scalar table instead of the general O(nd) SAGA table.

## • A convergence analysis aligned with the algorithm.

Relative smoothness controls Bregman descent, local component smoothness controls the SAGA table variance, and the safeguard closes the resulting Lyapunov recursion. The entire argument is carried out in a general finite-dimensional normed space, relying only on the dual norm, dual-norm Young inequalities, and the three-point Bregman inequality: it never invokes the Euclidean polarization identity or the norm decomposability that Euclidean adaptive variance-reduced analyses rest on. This chain yields an $O ( n / K )$ ergodic rate for convex objectives, a restarted linear rate under relative quadratic growth, and an $O ( 1 / K )$ bound on the expected squared Bregman proximal residual for nonconvex objectives. Matching these Euclidean-SAGA orders under the weaker non-Hilbert geometry—rather than improving them—is the technical content of the analysis. The convex ergodic and restarted-linear guarantees moreover have no counterpart in the SAGA/SARAH-based stochastic Bregman methods we build on, which are analyzed only in the nonconvex regime [42, 43].

## • Experiments that test the design mechanism.

A curvature diagnostic shows why arithmetic averages of stochastic BB ratios can spike and how the mediant stabilizes the candidate. Across four logistic-regression datasets, Ada-BPSG reaches low objective gaps in fewer efective passes and remains substantially less sensitive to initialization than standard VR baselines. On a simplex-constrained Poisson inverse problem under the entropy kernel—an instance meeting every hypothesis of the analysis, in which variance reduction is genuinely needed—it exhibits the predicted convex rate in genuinely non-Euclidean geometry and, by adapting past the conservative worst-case safeguard, improves on every fixed-step baseline held to its a-priori guaranteed step by more than two orders of magnitude—a gain that carries over unchanged to a regionpooled Poisson unmixing of the real Samson hyperspectral scene; on sparse nonnegative matrix factorization it attains the lowest objective curves among the compared Bregman methods while avoiding the line search used by the accelerated baseline.

## 2 Algorithm

Ada-BPSG adds one curvature-adaptive scalar to the BPSG-SAGA update. The construction proceeds from the classical BB secant ratio, identifies why an arithmetic average of stochastic ratios remains unstable, and replaces that average by a safeguarded mediant computed from information already stored by SAGA.

## 2.1 Barzilai–Borwein step size

The BB method is a scalar quasi-Newton scheme: it approximates the local Hessian by $\eta _ { k } ^ { - 1 } I$ and chooses $\eta _ { k }$ to fit a secant equation. In the Euclidean setting, define the iterate and gradient diferences $s _ { k } : = x _ { k } - x _ { k - 1 }$ and $y _ { k } : = \nabla f ( x _ { k } ) - \nabla f ( x _ { k - 1 } )$ . Minimizing either $\| \eta _ { k } ^ { - 1 } s _ { k } - y _ { k } \| _ { 2 }$ or $\| s _ { k } - \eta _ { k } y _ { k } \| _ { 2 }$ gives the two classical rules [5]

$$
\eta _ { k } ^ { \mathrm { B B 1 } } = \frac { s _ { k } ^ { T } s _ { k } } { s _ { k } ^ { T } y _ { k } } \qquad \mathrm { a n d } \qquad \eta _ { k } ^ { \mathrm { B B 2 } } = \frac { s _ { k } ^ { T } y _ { k } } { y _ { k } ^ { T } y _ { k } } .\tag{2.1}
$$

The first and second expressions correspond to the two residual problems, respectively. Both estimate inverse curvature along the latest displacement; our construction follows the BB2 orientation because SAGA directly supplies component-gradient diferences.

## 2.2 Why stochastic BB ratios need stabilization

A BB ratio uses only one displacement and is therefore sensitive to sampled curvature. In the BB2 rule, a small $y _ { k } ^ { T } y _ { k }$ can produce an excessively large step, especially near a solution or along a low-curvature component. Denominator regularization and explicit bounds alleviate this behavior [10, 18, 30, 47], but an arithmetic mean over recent ratios can still be dominated by a single nearly singular denominator.

Figure 1 isolates this failure mode on the ijcnn1 dataset [12]. For the regularized logistic component

$$
f _ { i } ( \boldsymbol { x } ) = \log ( 1 + \exp ( - b _ { i } a _ { i } ^ { T } \boldsymbol { x } ) ) + \frac { \lambda } { 2 } \| \boldsymbol { x } \| _ { 2 } ^ { 2 } ,
$$

$a _ { i } \in \mathbb { R } ^ { d }$ is a feature vector and $b _ { i } \in \{ - 1 , + 1 \}$ is its label. The Hessian at epoch k is

$$
H _ { k } = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } p _ { i } ( x _ { k } ) a _ { i } a _ { i } ^ { T } + \lambda I , \qquad p _ { i } ( x ) : = \frac { \exp ( - b _ { i } a _ { i } ^ { T } x ) } { [ 1 + \exp ( - b _ { i } a _ { i } ^ { T } x ) ] ^ { 2 } } ,
$$

so $p _ { i } ( x )$ records the sample-dependent curvature weight. Even as these weights settle, the vanilla and arithmetic-mean stabilized BB rules in Figure 1b spike to $1 0 ^ { 3 } – 1 0 ^ { 4 }$ . In contrast, the mediant candidate stays within the smallest and smoothest range among the plotted rules. The diagnostic motivates a division of labor that runs through the paper: the mediant stabilizes the empirical curvature estimate, and the safeguard enforces the bounds used by the convergence analysis.

## 2.3 Stabilized Barzilai–Borwein candidate

For positive denominators, the mediant of ratios satisfies

$$
\frac { \sum _ { i } a _ { i } } { \sum _ { i } b _ { i } } = \sum _ { i } \frac { b _ { i } } { \sum _ { j } b _ { j } } \frac { a _ { i } } { b _ { i } } .
$$

Hence the mediant is a denominator-weighted arithmetic mean: a nearly singular denominator contributes little weight instead of dominating the aggregate.

![](images/84ae1788a639e120b6cf3d8ebb24044a2320d796e3c68131db5e8ad41ff6d84a.jpg)  
(a)

![](images/7e7feda8bbacb074d1279b904fdda1794438abb8891810c80bf7b2497a33e60e.jpg)  
(b)  
Figure 1: Stochastic curvature and BB step sizes on ijcnn1, averaged over four runs. Left: distribution of the component curvature weights $p _ { i } ( x _ { k } )$ . Right: step-size trajectories over 30 efective passes; the mediant-based rule suppresses the spikes produced by vanilla and arithmetic-mean BB variants.

To apply this idea to $\mathrm { S A G A }$ , let $\varphi _ { i } ^ { t }$ denote the most recent point stored for component i at iteration t, and let $\mathcal { W } _ { k }$ collect the component updates since the previous step-size refresh. Each record $( i , t ) \in \mathcal { W } _ { k }$ supplies the secant pair

$$
\begin{array} { r } { s _ { i , t } : = \varphi _ { i } ^ { t + 1 } - \varphi _ { i } ^ { t } , \qquad y _ { i , t } : = \nabla f _ { i } ( \varphi _ { i } ^ { t + 1 } ) - \nabla f _ { i } ( \varphi _ { i } ^ { t } ) . } \end{array}
$$

Algorithm 1 maintains the two window sums

$$
\xi _ { k } : = \sum _ { ( i , t ) \in \mathcal { W } _ { k } } | \langle y _ { i , t } , s _ { i , t } \rangle | , \qquad \delta _ { k } : = \sum _ { ( i , t ) \in \mathcal { W } _ { k } } \| y _ { i , t } \| _ { * } ^ { 2 } ,
$$

where $\langle \cdot , \cdot \rangle$ is the duality pairing and $\| \cdot \| _ { * }$ <sub>∗</sub> is the dual norm. The raw adaptive candidate is

$$
\widehat { \eta } _ { k } ^ { \mathrm { B B } } = \left\{ \frac { 1 } { \alpha } \frac { \xi _ { k } } { \delta _ { k } + \theta \xi _ { k } } , \xi _ { k } > 0 , \right.\tag{2.2}
$$

The absolute value makes the numerator nonnegative when a nonconvex component produces a negative secant pairing. The bufer $\theta > 0$ prevents denominator degeneracy and bounds the raw candidate by $1 / ( \alpha \theta ) ; \alpha > 0$ controls its scale. In Euclidean space, the construction reduces to an aggregated BB2-type ratio.

The safeguard maps this curvature estimate to

$$
q _ { k } = \mathrm { c l i p } _ { [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ] } ( \widehat { \eta } _ { k } ^ { \mathrm { B B } } ) : = \operatorname* { m i n } \{ \eta _ { \mathrm { m a x } } , \operatorname* { m a x } \{ \eta _ { \mathrm { m i n } } , \widehat { \eta } _ { k } ^ { \mathrm { B B } } \} \} ,\tag{2.3}
$$

where $0 < \eta _ { \mathrm { m i n } } \leqslant \eta _ { \mathrm { m a x } }$ . Algorithm 1 then uses the monotone update

$$
\eta _ { k } = \operatorname* { m a x } \{ \eta _ { k - 1 } , q _ { k } \} ,
$$

which preserves the interval and supplies the monotonicity needed by the convex Bregman telescope. Thus the raw ratio governs adaptation within the interval, while the safeguard supplies the stability invariant used in Section 3.

## 2.4 Main method

Algorithm 1 integrates the candidate into BPSG-SAGA. A batch $B _ { k }$ refreshes the corresponding table entries, the standard SAGA correction forms the unbiased estimator $\widetilde { \nabla } _ { k } .$ , and the same table diferences update $\xi _ { k }$ and $\delta _ { k }$ . These accumulators are reset every m iterations after refreshing the step size, so adaptation requires only scalar reductions beyond the gradient-table operations.

The parameters have separate, explicit roles: m is the refresh frequency, θ regularizes the denominator, α rescales the raw ratio, and $[ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ defines the stability range. The algorithm adapts within this range instead of following a prescribed diminishing schedule. When $\mathbf { E } = \mathbb { R } ^ { d }$ and $\begin{array} { r } { \psi ( x ) = \frac { 1 } { 2 } \| x \| _ { 2 } ^ { 2 } } \end{array}$ , the Bregman update becomes the Euclidean proximal update (1.2); we call this specialization SAGA-BB in the experiments.

## 2.5 Eficient implementation

The general SAGA table stores one d-dimensional gradient per component and therefore costs $O ( n d )$ memory. Linear-prediction losses have the form $f _ { i } ( x ) = \Psi _ { i } ( \langle a _ { i } , x \rangle )$ , where $a _ { i } \in \mathbb { R } ^ { d }$ is a data vector and $\Psi _ { i } : \mathbb { R } $ R is a scalar loss [48]. In this case, storing only $\left. a _ { i } , \varphi _ { i } ^ { k } \right.$ recovers both the gradient-table correction and the secant sums in (2.2). The implementation in Appendix A.2 consequently reduces the table memory to $O ( n )$ without changing the Ada-BPSG update.

This reduction follows from the factorization

$$
\nabla f _ { i } ( \varphi _ { i } ^ { k } ) = \Psi _ { i } ^ { \prime } ( \langle a _ { i } , \varphi _ { i } ^ { k } \rangle ) a _ { i } ,
$$

where $\Psi _ { i } ^ { \prime }$ denotes the derivative of the scalar loss. When component i is refreshed, its old and new scalar predictions determine both the SAGA gradient correction and its contributions to $\xi _ { k }$ and $\delta _ { k } \mathbf { \mathrm { ; } }$ ; the feature norm $\| a _ { i } \| _ { * }$ can be precomputed. Ada-BPSG therefore needs neither a separate curvature table nor a history of past iterates.

The same reuse preserves the computational profile of the underlying BPSG-SAGA iteration. For a mini-batch of size $b ,$ step-size adaptation adds two scalar accumulations per refreshed component and a constant-cost safeguard whenever the step size is updated. It introduces no extra component-gradient evaluations or proximal subproblems. This is the implementation counterpart of the method’s central design: the stochastic correction and the curvature estimate use the same local information, while the safeguard converts that information into the stable step-size sequence required by the analysis.

```latex
Algorithm 1 Ada-BPSG
1: Input: max iterations $K ,$ batch-size $b \in \{ 1 , \ldots , n \}$ , initial point $x _ { 0 } ,$ kernel $\psi$
initial step-size $\eta _ { 0 } \in [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ] .$ update frequency m $\geqslant 1$
BB scaling $\alpha > 0 ,$ BB bufer $\theta > 0 .$ , safeguard bounds $0 < \eta _ { \mathrm { m i n } } \leqslant$ η<sub>max</sub>
2: Initialize: $\varphi _ { i } ^ { 0 } = x _ { 0 } ,$ , gradient table $\{ \nabla f _ { i } ( \varphi _ { i } ^ { 0 } ) \} _ { i = 1 } ^ { n }$
$\begin{array} { r } { \mu _ { 0 } = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \nabla f _ { i } ( \varphi _ { i } ^ { 0 } ) , \xi _ { 0 } = 0 , } \end{array}$ and $\delta _ { 0 } = 0$
3: for $k = 0 , \ddot { 1 } , \dotsc , K - 1$ do
4: if $k > 0$ then
5: $\eta _ { k } = \eta _ { k - 1 }$
6: end if
7: if $k > 0$ and k mod $m = 0$ then
8: η = $\int \frac { 1 } { \alpha } \frac { \xi _ { k } } { \delta _ { k } + \theta \xi _ { k } } , \quad \xi _ { k } > 0 ,$
$^ { \prime } { } I k ^ { - } \ : \Bigr ] \eta _ { k - 1 } , \qquad \xi _ { k } = 0 ,$
9: $\eta _ { k } = \dot { \operatorname* { m a x } } \{ \eta _ { k - 1 } , \mathrm { c l i p } _ { [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ] } ( \widehat { \eta } _ { k } ) \}$
10: $\xi _ { k } = 0 , \delta _ { k } = 0$
11: end if
12: Randomly pick a batch $B _ { k }$ uniformly without replacement
13: For $i \in B _ { k } .$ , set $\varphi _ { i } ^ { k + 1 } = x _ { k }$ and update $\nabla f _ { i } ( \varphi _ { i } ^ { k + 1 } )$
14: For i /∈ $B _ { k } ,$ set $\varphi _ { i } ^ { k + 1 } = \varphi _ { i } ^ { k }$
15: $\begin{array} { r } { \widetilde { \nabla } _ { k } = \frac { 1 } { b } \sum _ { i \in B _ { k } } \left( \nabla f _ { i } ( \varphi _ { i } ^ { k + 1 } ) - \nabla f _ { i } ( \varphi _ { i } ^ { k } ) \right) + \mu _ { k } } \end{array}$
16: $x _ { k + 1 } = \arg$ min<sub>x</sub> $\begin{array} { r } { \left\{ h ( x ) + \langle \widetilde { \nabla } _ { k } , x \rangle + \frac { 1 } { \eta _ { k } } D _ { \psi } ( x , x _ { k } ) \right\} } \end{array}$
17: Compute $y _ { i } = \nabla { \dot { f } } _ { i } ( \varphi _ { i } ^ { k + 1 } ) - \nabla f _ { i } ( \varphi _ { i } ^ { k } )$ for $i \in B _ { k }$
18: Compute $s _ { i } = \varphi _ { i } ^ { k + 1 } - \varphi _ { i } ^ { k }$ for $i \in B _ { k }$
19: $\begin{array} { r } { \xi _ { k + 1 } = \xi _ { k } + \sum _ { i \in B _ { k } } | \langle y _ { i } , s _ { i } \rangle | } \end{array}$
20: $\begin{array} { r } { \delta _ { k + 1 } = \delta _ { k } + \sum _ { i \in B _ { k } } \| y _ { i } \| _ { * } ^ { 2 } } \end{array}$
21: $\begin{array} { r } { \underline { { \mu _ { k + 1 } } } = \mu _ { k } + \frac { 1 } { n } \sum _ { i \in B _ { k } } \left( \nabla f _ { i } ( \varphi _ { i } ^ { k + 1 } ) - \nabla f _ { i } ( \varphi _ { i } ^ { k } ) \right) } \end{array}$
22: end for
23: return $x _ { K }$
```

## 3 Convergence Analysis

The analysis follows the same chain as the algorithm. Relative smoothness converts the Bregman proximal update into descent, component smoothness controls the $\mathrm { S A G A }$ estimation error, and the safeguard keeps the adaptive step within the range that balances these two terms. We first define the geometry and the step-size invariant, then establish variance bounds for the convex and nonconvex regimes, and finally derive the convergence rates.

## 3.1 Preliminaries

Normed-space notation. Let E be a finite-dimensional real normed vector space with norm $\| \cdot \|$ , and let $\mathbf { E } ^ { \ast }$ be its dual space, the space of continuous linear functionals on E. For $y \in \mathbf { E } ^ { * }$ and $x \in \mathbf { E } , \langle y , x \rangle$ denotes their duality pairing, and

$$
\| y \| _ { * } : = \operatorname* { m a x } \{ \langle y , x \rangle : \ \| x \| \leqslant 1 \} .
$$

This expression defines the dual norm $\| \cdot \| _ { * }$ . The filtration $\mathcal { F } _ { k } : = \sigma ( B _ { 0 } , \ldots , B _ { k - 1 } )$ contains the mini-batches sampled before iteration k, and $\mathbb { E } _ { k } [ \cdot ] : = \mathbb { E } [ \cdot \mid \mathcal { F } _ { k } ]$ denotes conditional expectation given this history.

Since E is finite-dimensional, it is automatically complete, hence Banach; closed bounded subsets are compact, all norms are equivalent, and $\mathbf { E } ^ { \ast }$ has the same dimension. Every existence and boundedness fact used below—solvability of the proximal subproblem and existence of $x ^ { * } \in D$ follows from dim $\mathbf { E } < \infty$ together with the strong convexity of $\psi _ { : }$ , so no completeness or sequential-compactness assumption beyond finite-dimensionality is invoked. In fact the convergence results are non-asymptotic, finite-horizon bounds (Theorems 3.6–3.11) and use no limit-point or subsequence argument at all. The generality relative to the Euclidean setting is therefore geometric—the norm need not derive from an inner product, so no polarization identity or norm decomposition is available—rather than topological.

Bregman geometry. A distance-generating function (DGF), or Bregman kernel, is a continuously diferentiable function $\psi : \mathbf { E }  \mathbb { R }$ that is 1-strongly convex with respect to $\| \cdot \| ;$

$$
\psi ( x ) - \psi ( y ) - \langle \nabla \psi ( y ) , x - y \rangle \geqslant { \frac { 1 } { 2 } } \| x - y \| ^ { 2 } .
$$

Its Bregman divergence is the first-order linearization error

$$
D _ { \psi } ( x , y ) : = \psi ( x ) - \psi ( y ) - \langle \nabla \psi ( y ) , x - y \rangle .
$$

Strong convexity gives $D _ { \psi } ( x , y ) \geqslant \frac 1 2 \| x - y \| ^ { 2 }$ , which connects Bregman descent to the norm diferences used in the $\mathrm { S A G A }$ variance bounds. Given a direction $\mathcal { G } \in \mathbf { E } ^ { * }$ and step size $\eta > 0$ define the Bregman proximal mapping by

$$
\mathrm { P r o x } _ { h , \eta } ^ { \psi } ( x , \mathcal { G } ) : = \arg \operatorname* { m i n } _ { u \in \mathbf { E } } \left\{ \langle \mathcal { G } , u \rangle + \frac { 1 } { \eta } D _ { \psi } ( u , x ) + h ( u ) \right\}
$$

whenever the minimizer exists. Assumption 3.4 ensures that this mapping is single-valued along the algorithmic trajectory.

Definition 3.1 (Smooth adaptability [8, 29, 43]). Let $\mathcal { X } \subset \mathbf { E }$ be nonempty, convex, and open, and let $f : \mathcal { X } \to \left( - \infty , + \infty \right]$ be proper, lower semicontinuous, and continuously diferentiable on $\mathcal { X }$ . Given a DGF $\psi ,$ , the pair $( f , \psi )$ is $( \bar { L } , \underline { { L } } )$ -smooth adaptable on X if constants $\bar { L } > 0$ and $\underline { { L } } \geqslant 0$ satisfy, for every $x , y \in { \mathcal { X } }$

$$
- \underline { { L } } D _ { \psi } ( x , y ) \leqslant f ( x ) - f ( y ) - \langle \nabla f ( y ) , x - y \rangle \leqslant \bar { L } D _ { \psi } ( x , y ) .
$$

The upper inequality is the relative-smoothness bound used for descent; the lower inequality controls negative curvature. The convergence proofs below use the upper constant $\bar { L }$ on a deterministic region $D \subset { \mathcal { X } }$

Example 3.2 (A genuinely non-Euclidean instance). Let $\mathbf { E } = \mathbb { R } ^ { d }$ carry the norm $\| \cdot \| = \| \cdot \| _ { 1 }$ so that the dual norm is $\| \cdot \| _ { * } = \| \cdot \| _ { \infty }$ , and let $h = \iota _ { \Delta }$ be the indicator of the probability simplex $\Delta = \{ x \geq 0 : \textstyle \sum _ { i } x _ { i } = 1 \}$ . The negative-entropy kernel $\textstyle \psi ( x ) = \sum _ { i }$ x<sub>i</sub> log $x _ { i }$ is 1-strongly convex with respect to $\| \cdot \| _ { 1 }$ by Pinsker’s inequality, and its divergence is the Kullback–Leibler divergence $\begin{array} { r } { D _ { \psi } ( x , y ) = \sum _ { i } x _ { i } \log ( x _ { i } / y _ { i } ) } \end{array}$ , which is asymmetric and admits no polarization identity. For a finite sum $\begin{array} { r } { f = \frac { 1 } { n } \sum _ { i } f _ { i } } \end{array}$ with bounded Hessians on $\Delta .$ , the pair $( f , \psi )$ is relatively smooth; the SAGA correction $\dot { \nabla } _ { k }$ and the accumulators $\begin{array} { r } { \xi _ { k } = \sum | \langle y _ { i , t } , s _ { i , t } \rangle | } \end{array}$ and $\delta _ { k } = \sum \| y _ { i , t } \| _ { \infty } ^ { 2 }$ are then measured in $\ell _ { \infty }$ , and the proximal step is the KL projection onto $\Delta$ , i.e. a softmax renormalization. Every constant in Theorems 3.6–3.11 is expressed through the $\ell _ { 1 } / \ell _ { \infty }$ pairing, with $\sigma _ { b } \geqslant 1$ typically strict; the Euclidean specialization $\begin{array} { r } { \psi = \frac { 1 } { 2 } \| \cdot \| _ { 2 } ^ { 2 } } \end{array}$ used in Section 4 is the degenerate case $\| \cdot \| = \| \cdot \| _ { * } = \| \cdot \| _ { 2 } , \sigma _ { b } = 1$ . Standard Euclidean-underlying stochastic Bregman analyses, which measure gradients and variance in $\ell _ { 2 }$ , do not cover this instance directly.

## 3.2 Safeguarded step-size framework

The candidate $\widehat { \eta } _ { k } ^ { \mathrm { B B } }$ , its clipped value $q _ { k }$ , and the monotone update $\eta _ { k } = \operatorname* { m a x } \{ \eta _ { k - 1 } , q _ { k } \}$ are defined in $( 2 . 2 ) \ - ( 2 . 3 )$ . Because they use only records collected before $B _ { k }$ is sampled, $\eta _ { k }$ is $\mathcal { F } _ { k } \mathrm { { - m e a s u r a b l e } }$ . The update maintains the deterministic invariant

$$
\eta _ { \mathrm { m i n } } \leqslant \eta _ { k } \leqslant \eta _ { \mathrm { m a x } } , \qquad \eta _ { k } \geqslant \eta _ { k - 1 } \quad \mathrm { f o r ~ a l l ~ } k .
$$

The lower bound sets the scale in the convex rate, the upper bound absorbs relative smoothness and gradient-estimation error, and monotonicity telescopes the Bregman distance in the convex proof. The nonconvex argument uses only the upper bound.

Remark 3.3 (Cost of monotonicity). The nondecreasing rule $\eta _ { k } = \operatorname* { m a x } \{ \eta _ { k - 1 } , q _ { k } \}$ is precisely what makes $a _ { k } = 1 / \eta _ { k }$ nonincreasing and thus lets the Bregman distance telescope in Theorem 3.6. Its price is that the step may rise but never fall: starting from $\eta _ { \mathrm { m i n } } ,$ it ramps toward the clipped curvature estimate and then holds. Overshoot is controlled not by retreat but by the fixed cap $\eta _ { \mathrm { m a x } } ,$ so the adaptation is confined to how fast and how far the step climbs within $[ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ . The nonconvex analysis (Theorem 3.11) does not use monotonicity and relies only on the upper bound, so there the step is free to vary in both directions inside the interval.

## 3.3 Assumptions

Assumption 3.4. Items 1–5 apply to all results; Item 6 is used only in the convex analysis.

1. Geometry and prox. The function ψ is continuously diferentiable and 1-strongly convex with respect to $\| \cdot \|$ . The function $h : \mathbf { E }  ( - \infty , + \infty ]$ is proper, closed, and convex. For every $x \in D , \mathcal { G } \in \mathbf { E } ^ { * }$ , and $\eta \in [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ , the Bregman proximal subproblem has a unique solution.

2. Deterministic bounded region. There exists a deterministic bounded convex set $D \subset { \mathcal { X } }$ such that $x _ { k } , \varphi _ { i } ^ { k } \in D$ almost surely for all k and i. In the convex results, an optimal solution $x ^ { * }$ also belongs to $D$

3. Sampling. Conditional on $\mathcal { F } _ { k }$ , the mini-batch is sampled uniformly from [n] with size b and without replacement. Write $I _ { 1 } , \ldots , I _ { b }$ for the sampled indices. A deterministic mini-batch second-moment constant $\sigma _ { b } \geqslant 1$ satisfies, for any deterministic $v _ { 1 } , \dots , v _ { n } \in \mathbf { E } ^ { * }$ with $\begin{array} { r } { \bar { v } = \frac { 1 } { n } \sum _ { i } v _ { i } } \end{array}$

$$
\mathbb { E } \left[ \left. \frac { 1 } { b } \sum _ { j = 1 } ^ { b } ( v _ { I _ { j } } - \bar { v } ) \right. _ { * } ^ { 2 } \right] \leqslant \frac { \sigma _ { b } } { b n } \sum _ { i = 1 } ^ { n } \| v _ { i } - \bar { v } \| _ { * } ^ { 2 } .
$$

In the Euclidean norm, one may take $\sigma _ { b } = 1$ . Conditional on $\mathcal { F } _ { k }$ , the table vectors are fixed, so this inequality applies directly to the SAGA estimator.

4. Relative smoothness. The pair $( f , \psi )$ is $( \bar { L } , \underline { { L } } )$ -smooth adaptable on $D ;$ only the upper bound with L<sup>¯</sup> is used in the convergence proofs. This is a relative smoothness condition for the aggregate diferentiable term f with respect to the chosen kernel $\psi$

5. Component local smoothness. Each $\nabla f _ { i }$ is Lipschitz on D with deterministic constant $M , i . e .$ 2

$$
\| \nabla f _ { i } ( x ) - \nabla f _ { i } ( y ) \| _ { * } \leqslant M \| x - y \| , \qquad x , y \in D , \ i \in [ n ] .
$$

6. Convex self-bounding condition. For the convex results, each $f _ { i }$ is convex on D and satisfies

$$
\begin{array} { r } { \| \nabla f _ { i } ( x ) - \nabla f _ { i } ( y ) \| _ { * } ^ { 2 } \leqslant 2 M \left( f _ { i } ( x ) - f _ { i } ( y ) - \langle \nabla f _ { i } ( y ) , x - y \rangle \right) , \qquad x , y \in D . } \end{array}
$$

Role of the assumptions. The deterministic set D localizes the component smoothness needed for variance reduction. A concrete verifiable suficient condition is that dom h is compact and the kernel $\psi$ is continuous on dom h: the Bregman proximal subproblem then minimizes a lower-semicontinuous function over a compact feasible set, so every iterate $x _ { k }$ and every table point $\varphi _ { i } ^ { k }$ stays in $D : = \operatorname { d o m } h$ almost surely, and M is the finite Lipschitz constant of the component gradients on that compact set. This makes Item 2 an assumption on the problem data rather than on the trajectory. The sparse-NMF experiment of Section 4 does not satisfy this out of the box, because the objective is invariant under the rescaling $( U , V ) \mapsto ( t U , t ^ { - 1 } V )$ and its sublevel sets are therefore unbounded; appending an explicit box $0 \leq U , V \leq \mathcal { U }$ to the cardinality and nonnegativity constraints restores a compact D while leaving the optimal value unchanged for U large enough, at the cost of enforcing the box in the proximal step. Fixing D before the run keeps M deterministic. This local component condition serves a diferent purpose from relative smoothness: L<sup>¯</sup> controls descent of the aggregate $f$ in Bregman geometry, whereas M controls diferences between stored component gradients.

For convex components, Item 6 converts those gradient diferences into the linearization errors used by the objective-gap Lyapunov function. Convex Euclidean M-smooth functions satisfy this self-bounding inequality automatically. The nonconvex analysis instead tracks distances between the current iterate and the SAGA table points, and therefore uses only Items 1–5.

## 3.4 Variance bounds

For the convex analysis, let $x ^ { * }$ be an optimal solution in D and choose $\rho ^ { * } \in \partial h ( x ^ { * } )$ satisfying the first-order condition

$$
\nabla f ( x ^ { * } ) + \rho ^ { * } = 0 .
$$

Define

$$
\Delta _ { i } ( x ) : = f _ { i } ( x ) - f _ { i } ( x ^ { * } ) - \langle \nabla f _ { i } ( x ^ { * } ) , x - x ^ { * } \rangle , \qquad H ( x ) : = h ( x ) - h ( x ^ { * } ) - \langle \rho ^ { * } , x - x ^ { * } \rangle .
$$

Here $\Delta _ { i } ( x )$ is the linearization error of component $f _ { i } , H ( x )$ is the corresponding error of $h ,$ and $\begin{array} { r } { \Delta ( x ) : = \frac { 1 } { n } \sum _ { i } \Delta _ { i } ( x ) } \end{array}$ . Convexity makes all three quantities nonnegative; the optimality condition also gives $F ( x ) - F ( x ^ { * } ) = \Delta ( x ) + H ( x )$ . This identity links the SAGA variance to the objective gap.

Lemma 3.5 (Convex SAGA variance). Under Assumption ${ 3 . 4 } ,$ the SAGA estimator in Algorithm 1 is conditionally unbiased,

$$
\mathbb { E } _ { k } [ \widetilde { \nabla } _ { k } ] = \nabla f ( x _ { k } )
$$

and its conditional variance in the convex setting satisfies

$$
\mathbb { E } _ { k } \left[ \| \widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) \| _ { * } ^ { 2 } \right] \leqslant \frac { 1 6 \sigma _ { b } M } { b } \left[ \Delta ( x _ { k } ) + \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \Delta _ { i } ( \varphi _ { i } ^ { k } ) \right] .
$$

## 3.5 Convex analysis

Lemma 3.5 makes the stochastic error proportional to the current and stored objective gaps. Combining this estimate with the three-point Bregman proximal inequality yields a Lyapunov decrease in which the safeguard absorbs the variance term.

Theorem 3.6. Suppose Assumption $\ 3 . 4$ holds in the convex setting, and the safeguarded step-sizes satisfy

$$
\eta _ { \mathrm { m i n } } \leqslant \eta _ { k } \leqslant \eta _ { \mathrm { m a x } } , \qquad \eta _ { k } \geqslant \eta _ { k - 1 } , \qquad \eta _ { \mathrm { m a x } } \leqslant \frac { 1 } { \bar { L } + 3 2 \sigma _ { b } M / b } .
$$

Let $\begin{array} { r } { \bar { x } _ { K } = \frac { 1 } { K } \sum _ { k = 1 } ^ { K } x _ { k } } \end{array}$ . Then

$$
\mathbb { E } [ F ( \bar { x } _ { K } ) ] - F ( x ^ { * } ) \leqslant \frac { 2 } { K } \left[ \left( \frac { n } { 4 b } + \frac { 1 } { 2 } \right) ( F ( x _ { 0 } ) - F ( x ^ { * } ) ) + \frac { 1 } { \eta _ { \operatorname* { m i n } } } D _ { \psi } ( x ^ { * } , x _ { 0 } ) \right] .
$$

Theorem 3.6 therefore gives an $O ( n / K )$ ergodic rate for fixed b. Its dependence on η<sub>min</sub> records the cost of the lower safeguard, while the admissible $\eta _ { \mathrm { m a x } }$ couples relative smoothness and SAGA variance. The BB candidate remains free to adapt anywhere inside this interval.

Remark 3.7 (What the guarantee does and does not claim). The admissible cap $\eta _ { \mathrm { m a x } } \leq ( \bar { L } +$ $3 2 \sigma _ { b } M / b ) ^ { - 1 }$ encodes exactly the smoothness constants L<sup>¯</sup> and M that a well-tuned fixed step would use. Consequently Theorem 3.6 does not improve on the $O ( n / K )$ rate of fixed-step BPSG $\operatorname { S A G A } ;$ it certifies that adapting within $[ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ does not degrade that rate. Ada-BPSG is thus adaptive within a safe interval whose upper end still depends on problem constants, not free of them; the empirical payof (Section 4) is robustness to the initial step size rather than a faster worst-case rate. The leading constant is moreover governed by $\eta _ { \mathrm { m i n } } ^ { - 1 } D _ { \psi } ( x ^ { * } , x _ { 0 } )$ , so a conservative $\eta _ { \mathrm { m i n } }$ enlarges the bound: the BB candidate removes the need to guess where in $[ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ the step should sit, but not the need to set the interval from L<sup>¯</sup> and M. The analysis is nonetheless conducted entirely in the general normed space E: it uses only the dual norm, dual-norm Young inequalities, and the three-point identity of Lemma $\mathrm { A . 1 }$ , and at no point invokes the Euclidean polarization identity $2 \langle a , b \rangle = \| a \| ^ { 2 } + \| b \| ^ { 2 } - \| a - b \| ^ { 2 }$ or the norm decomposability on which the Euclidean adaptive-BB analyses rely. The price of dropping them is explicit rather than hidden: the mini-batch second-moment constant obeys $\sigma _ { b } \geqslant 1$ with equality only in the Euclidean norm (Assumption 3.4, Item 3), and the variance bound of Lemma 3.5 carries a factor 4 where the Hilbert variance identity $\begin{array} { r } { \sum _ { i } \| a _ { i } - \bar { a } \| ^ { 2 } = \sum _ { i } \| a _ { i } \| ^ { 2 } - n \| \bar { a } \| ^ { 2 } } \end{array}$ would give 1. Recovering the Euclidean $O ( n / K )$ order in this non-Hilbert geometry, rather than improving it, is where the dificulty lies, so the contribution is added generality at no loss of rate.

Relative quadratic growth means that the objective gap controls the Bregman distance to a solution. Under this condition, restarting the averaged convex guarantee turns a constant-factor reduction per round into geometric convergence.

Theorem 3.8. In addition to the assumptions of Theorem ${ \it 3 . 6 , }$ suppose F satisfies the relative quadratic growth condition

$$
F ( x ) - F ( x ^ { * } ) \geqslant \mu D _ { \psi } ( x ^ { * } , x ) , \qquad x \in D .
$$

Let

$$
A _ { b } : = { \frac { n } { 4 b } } + { \frac { 1 } { 2 } } .
$$

If each restart round reinitializes the SAGA table at $x ^ { ( t ) }$ , starts or continues with a step-size in $[ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ]$ satisfying the monotonicity condition within the round, and runs Theorem 3.6 for

$$
K \geqslant 4 \left( A _ { b } + \frac { 1 } { \mu \eta _ { \mathrm { m i n } } } \right)
$$

iterations and sets $x ^ { ( t + 1 ) }$ to the averaged iterate of round t, then after τ restart rounds,

$$
\begin{array} { r } { \mathbb { E } [ F ( x ^ { ( \tau ) } ) ] - F ( x ^ { * } ) \leqslant 2 ^ { - \tau } \left( F ( x ^ { ( 0 ) } ) - F ( x ^ { * } ) \right) . } \end{array}
$$

The same condition converts the objective contraction into a Bregman distance bound:

$$
\mathbb { E } \big [ D _ { \psi } ( x ^ { * } , x ^ { ( \tau ) } ) \big ] \leqslant \frac { 2 ^ { - \tau } } { \mu } \Big ( F ( x ^ { ( 0 ) } ) - F ( x ^ { * } ) \Big ) .
$$

If the kernel is 1-strongly convex, this further bounds the squared norm distance by $\mathbb { E } \lVert x ^ { ( \tau ) }$ $x ^ { * } \| ^ { 2 } \leqslant 2 \mathbb { E } [ D _ { \psi } ( x ^ { * } , x ^ { ( \tau ) } ) ]$ whenever the displayed Bregman distance is evaluated on D.

## 3.6 Nonconvex analysis

We next allow $f$ to be nonconvex and assume that $F$ has a finite lower bound $F _ { \mathrm { i n f } }$ . Because an objective gap alone does not characterize stationarity, we use a Bregman proximal gradient mapping.

Definition 3.9 (Bregman proximal gradient mapping). For $\eta > 0$ , define

$$
\mathcal { G } _ { \eta } ( x ) = \frac { x - \mathrm { P r o x } _ { h , \eta } ^ { \psi } ( x , \nabla f ( x ) ) } { \eta } .
$$

Let

$$
\widetilde { \boldsymbol { x } } _ { k + 1 } = \mathrm { P r o x } _ { h , \eta _ { k } } ^ { \psi } ( \boldsymbol { x } _ { k } , \boldsymbol { \nabla } f ( \boldsymbol { x } _ { k } ) ) .
$$

The full-gradient proximal point $\widetilde { x } _ { k + 1 }$ removes the SAGA estimation error from the stationarity measure. We normalize its displacement by the fixed upper safeguard:

$$
\mathcal { R } _ { k } : = \frac { x _ { k } - \widetilde { x } _ { k + 1 } } { \eta _ { \operatorname* { m a x } } } .
$$

This normalization yields a common scale across adaptive iterations. The residual $\mathcal { R } _ { k }$ vanishes exactly when the full-gradient Bregman proximal step leaves $x _ { k }$ unchanged.

Lemma 3.10 (Nonconvex SAGA variance). Under Items 1–5 of Assumption $3 . 4 ,$

$$
\mathbb { E } _ { k } \left[ \| \widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) \| _ { * } ^ { 2 } \right] \leqslant \frac { 4 \sigma _ { b } M ^ { 2 } } { b n } \sum _ { i = 1 } ^ { n } \| x _ { k } - \varphi _ { i } ^ { k } \| ^ { 2 } .
$$

Theorem 3.11. Suppose the common conditions in Assumption $\ 3 . 4$ hold, f may be nonconvex, and $F$ is bounded below by $F _ { \mathrm { i n f } } > - \infty$ . Let

$$
\Gamma _ { b } : = \frac { 8 \sigma _ { b } M ^ { 2 } n ( 2 n - b ) } { b ^ { 3 } } .
$$

If

$$
\Gamma _ { b } \eta _ { \mathrm { m a x } } + \bar { L } \leqslant \frac { 1 } { \eta _ { \mathrm { m a x } } } ,
$$

then for R drawn independently and uniformly from $\{ 0 , \ldots , K - 1 \}$ ,

$$
\mathbb { E } \left[ \Vert \mathcal { R } _ { R } \Vert ^ { 2 } \right] \leqslant \frac { 2 ( F ( x _ { 0 } ) - F _ { \mathrm { i n f } } ) } { K \eta _ { \mathrm { m a x } } } .
$$

Theorem 3.11 gives an $O ( 1 / K )$ bound on the expected squared residual. The quantity $\Gamma _ { b }$ captures how mini-batch sampling and table refresh interact with local component smoothness, while the upper safeguard balances this variance term against relative smoothness. Because $\Gamma _ { b } = O ( M ^ { 2 } n ^ { 2 } / b ^ { 3 } )$ , the stability condition $\Gamma _ { b } \eta _ { \mathrm { m a x } } + \bar { L } \leq \eta _ { \mathrm { m a x } } ^ { - 1 }$ forces $\eta _ { \mathrm { m a x } } = { O ( b ^ { 3 / 2 } / ( n M ) ) }$ so the residual bound $2 ( F ( x _ { 0 } ) - F _ { \mathrm { i n f } } ) / ( K \eta _ { \mathrm { m a x } } )$ inherits the $O ( n )$ -type gradient complexity characteristic of SAGA rather than the improved $O ( { \sqrt { n } } )$ dependence available to SARAH- or $\mathrm { S P I D E R - t y p e }$ recursive estimators. We nonetheless build on SAGA because its table supplies the component secant pairs $( s _ { i , t } , y _ { i , t } )$ directly, which is what the stabilized candidate consumes; porting the same candidate to a memory-light recursive estimator is left to future work. Section 4 examines how the stabilized candidate behaves with practically chosen safeguards and kernels.

## 4 Numerical Experiments

The experiments probe the mechanisms developed above: whether the adaptive SAGA step improves gradient eficiency, whether stabilization reduces sensitivity to the initial scale, whether the convex $O ( n / K )$ guarantee is realized inside a genuinely non-Euclidean instance that meets every hypothesis of Assumption 3.4, whether the same behavior transfers from synthetic to real measured data, and whether the design remains efective on a harder non-Euclidean problem that falls outside the theory. We use $\mathbf { E } = \mathbb { R } ^ { d }$ throughout and vary the distance-generating function $\psi$ with the problem geometry.

## 4.1 Regularized binary classification

We first choose the Euclidean kernel $\begin{array} { r } { \psi ( x ) = \frac { 1 } { 2 } \| x \| _ { 2 } ^ { 2 } } \end{array}$ and solve ℓ<sub>2</sub>-regularized logistic regression,

$$
F ( \boldsymbol { x } ) = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \log ( 1 + \exp ( - b _ { i } a _ { i } ^ { T } \boldsymbol { x } ) ) + \frac { \lambda } { 2 } \Vert \boldsymbol { x } \Vert _ { 2 } ^ { 2 } , \qquad \lambda = \frac { 1 } { n } ,
$$

where $a _ { i } \in \mathbb { R } ^ { d }$ and $b _ { i } \in \{ - 1 , + 1 \}$ are the features and label of sample i. Under this kernel, Ada-BPSG specializes to SAGA-BB. We compare it with SAGA, SVRG, SVRG-BB, loopless SVRG, and SARAH on mushrooms, ijcnn1, w8a, and covtype from LibSVM [12]. Every method uses the same tuning grid: the initial step size lies in $\{ 1 0 ^ { - 3 } , 1 0 ^ { - 2 } , 1 0 ^ { - 1 } , 1 , 1 0 , \overset { \cdot } { 1 0 0 } \}$ and the batch size lies in {1, 8, 16, 64}. For methods with an epoch length or refresh interval, we set it to $m = n / b$

Figure 2 connects the curvature diagnostic in Figure 1 to optimization performance. SAGA-BB reaches the smallest plotted objective gaps with the fewest efective passes on all four datasets. The gain is especially pronounced on mushrooms, w8a, and covtype, where its curve separates early from both SAGA and the SVRG-family baselines. Thus the stabilized secant information does more than prevent isolated step-size spikes: it improves the gradient eficiency of the underlying SAGA update.

The initial-scale experiment in Figure 3 fixes $b = 8$ and measures the final gradient norm after 120 efective passes. The vanilla VR methods vary by several orders of magnitude across the grid and can become unstable at its extremes. SVRG-BB reduces this dependence on some datasets but remains uneven on w8a. SAGA-BB maintains a low gradient norm across the entire grid on every dataset. This behavior matches the algorithmic design: the initial value governs only the iterations before the first refresh, after which the mediant candidate adapts within the safeguard interval. Appendix B verifies the same pattern across additional batch sizes and with Huber loss.

## 4.2 Simplex-constrained Poisson inverse problem

The second experiment moves to a non-Euclidean kernel while staying strictly inside the theory, and is chosen so that variance reduction is genuinely needed. We recover mixing weights on the probability simplex from count observations,

$$
\operatorname* { m i n } _ { x \in \Delta } \ F ( x ) = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \Big ( \langle a _ { i } , x \rangle - b _ { i } \log \langle a _ { i } , x \rangle \Big ) , \qquad \Delta = \Big \{ x \in \mathbb { R } ^ { d } \colon x \geqslant 0 , \ \sum _ { j } x _ { j } = 1 \Big \} ,\tag{4.1}
$$

where the rows $a _ { i } > 0$ are known nonnegative dictionary atoms and $b _ { i } \sim$ Poisson $\left( \left. a _ { i } , x ^ { \star } \right. \right)$ are counts generated by a planted simplex vector $x ^ { \star }$ . Each $f _ { i } ( x ) = \Psi _ { i } ( \langle a _ { i } , x \rangle )$ with $\Psi _ { i } ( u ) = u - b _ { i }$ log u is a convex linear-prediction loss, so the $O ( n )$ scalar-table implementation of Appendix A.2 applies verbatim, and $h = \iota _ { \Delta }$ is the indicator of the simplex. Following Example 3.2 we equip $\mathbf { E } = \mathbb { R } ^ { d }$ with the $\ell _ { 1 }$ norm, its dual $\ell _ { \infty }$ , and the negative-entropy kernel $\begin{array} { r } { \psi ( x ) = \sum _ { j } x _ { j } \log x _ { j } } \end{array}$ which is 1-strongly convex with respect to $\| \cdot \| _ { 1 } ;$ the Bregman proximal step of Algorithm 1 then has the closed form of an entropic mirror update,

$$
x _ { k + 1 , j } = \frac { x _ { k , j } \exp ( - \eta _ { k } [ \widetilde { \nabla } _ { k } ] _ { j } ) } { \sum _ { l } x _ { k , l } \exp ( - \eta _ { k } [ \widetilde { \nabla } _ { k } ] _ { l } ) } ,
$$

and the accumulators $\xi _ { k } , \delta _ { k }$ are formed in the $\ell _ { \infty }$ dual norm. Because the atoms are strictly positive, the prediction obeys $\begin{array} { r } { \langle a _ { i } , x \rangle \geqslant \underline { { p } } : = \operatorname* { m i n } _ { i } \operatorname* { m i n } _ { j } a _ { i j } > 0 } \end{array}$ on the simplex, which keeps the loss away from the singularity of log and makes every constant in Assumption 3.4 finite: $\Delta$ is compact and convex, so the deterministic region $D = \Delta$ is available a priori; each $\Psi _ { i }$ is convex with $\Psi _ { i } ^ { \prime \prime } ( u ) = { b _ { i } } / { u ^ { 2 } }$ , giving the component-gradient bound $M = ( \operatorname* { m a x } _ { i } b _ { i } / p ^ { 2 } )$ max<sub>i</sub> $\| a _ { i } \| _ { \infty } ^ { 2 }$ of Items 5–6 and a finite relative-smoothness scale L<sup>¯</sup> against the entropy kernel. Crucially, unlike a quadratic data term, the Poisson loss has no global Euclidean Lipschitz gradient, so constant-step SGD retains a variance floor while a variance-reduced step can drive the gap arbitrarily low: this is exactly the regime the method targets, and the convex rate of Theorem 3.6 is expected to hold in it. The geometry is non-Hilbert throughout, with $\sigma _ { b } > 1$ in general.

![](images/b76058c084281810ba2d542494ce757552af6228b4e7bb0c9b426109384dc3f0.jpg)  
(a)

![](images/9618143f56e21d1fc6919c93b7f2763859f7622fa490d5028eda44d3d78edfe5.jpg)  
(b)

![](images/28808d913289f8e62534253e7e22670db8d70f04ae20b253a98390dc4d71a2eb.jpg)  
(c)

![](images/1d7556dd3e008ef7733ee45df59ce4607608612ab2a06197528157722f9dd081.jpg)  
(d)

Figure 2: Gradient eficiency for regularized logistic regression. The horizontal axis counts componentgradient evaluations normalized by n, and the vertical axis is the objective gap to the best value attained in the corresponding experiment. Lines and shaded regions show the mean and standard deviation over five runs. All methods use their best configuration from the common tuning grid.  
![](images/85acefb59548cdf738853116cea10b6e95ad0b76efaa4631a6080ae53f6a49de.jpg)  
(a)

![](images/0dc54cd2d41fa0e310ed43b6f471304e60354abde1ce19b43e8d8fffefed5125.jpg)  
(b)

![](images/6456b63604b84544429e55f74a3bb920c2de87563208fa7e74cea435990b8838.jpg)  
(c)

![](images/c1e10fd3c23119958398d8cf104d8a0da055d3c8100d511ba51d1070a1cd02cf.jpg)  
(d)  
Figure 3: Sensitivity to the initial step size after 120 efective passes with batch size $b = 8$ . Each part reports the final gradient norm; finite runs within the displayed range are shown.

We use a controlled synthetic instance with $d = 5 0 0 , n = 5 0 0 0$ , atoms $a _ { i }$ with entries drawn uniformly in [0.05, 1.05], a planted interior signal $x ^ { \star } .$ , and Poisson counts $b _ { i } ;$ the batch size is $b = 8$ and the refresh interval is $m = n / b$ . Because the problem is convex, we obtain the reference value $F ^ { \star }$ from an independent high-accuracy full-batch entropic mirror descent with backtracking, so that no plotted stochastic method is scored against its own best iterate. We compare Ada-BPSG against deterministic $\mathrm { B P G }$ , against BPSG with SGD, SAGA, and SARAH estimators, and against the extrapolated line-search variant BPSGE. The decisive feature of this instance is that its worst-case component constant M is dominated by the log singularity and is therefore far larger than the curvature the iterates actually visit: here L<sup>¯</sup> ≈ 18 but $M \approx 2 . 2 \times 1 0 ^ { 3 }$ whereas the local component smoothness at typical predictions is only $M _ { \mathrm { l o c } } \approx 1 8$ . Figure 4 (a)–(b) therefore adopt an a-priori protocol: every fixed-step baseline uses the largest step its own analysis guarantees from these quantities— $( \bar { L } + M ) ^ { - 1 }$ for SGD, $( \bar { L } + 3 2 M / b ) ^ { - 1 }$ for SAGA, and $( \bar { L } + \bar { M } / b ) ^ { - 1 }$ for the recursive SARAH estimator—while Ada-BPSG adapts within a safeguard $\eta _ { \mathrm { m a x } } = ( \bar { L } + M _ { \mathrm { l o c } } / b ) ^ { - 1 }$ built from the local constant, started from the lower safeguard $\eta _ { \mathrm { m i n } }$ . This local cap exceeds the worst-case safeguard $( \bar { L } + 3 2 \sigma _ { b } M / b ) ^ { - 1 }$ of Theorems 3.6–3.11, so it is a heuristic relaxation rather than a theory-faithful setting; to make the gap explicit we also plot “Ada-BPSG (theory cap)”, the same rule confined to the analysis cap with the global M. The complementary question—what a baseline achieves if its step is instead hand-tuned per instance—is answered separately in Figure 4 (c).

Figure 4 reads along the same three axes as the classification study, now in Bregman geometry. In Figure 4 (a) the gap of Ada-BPSG decays steadily to $1 . 3 \times 1 0 ^ { - 5 }$ along the $O ( n / K )$ reference slope, exhibiting the convex-rate behavior predicted by Theorem 3.6 in a bona fide non-Euclidean instance. The fixed-step baselines, held to the steps their own theory licenses, remain near $1 0 ^ { - 2 }$ because the worst-case M is inflated by the log singularity, the guaranteed steps are two to four orders of magnitude below the useful curvature scale, and the methods barely move in 80 passes. The “theory $\mathrm { c a p } ^ { \mathrm { , } \mathrm { , } }$ run of Ada-BPSG, confined to the same worst-case safeguard, sits with them—so on this instance the analysis-faithful step is itself conservative, and the speedup comes precisely from adapting beyond it. This is the intended behavior of the stabilized rule: its secant candidate climbs from $\eta _ { \mathrm { m i n } }$ to the local safeguard, improving on the best a-priori baseline by more than two orders of magnitude while reading only the SAGA information it already maintains, and it does so as a heuristic relaxation of the worst-case cap, as is standard for BB-type steps. Figure 4 (b) shows the same separation in wall-clock time: Ada-BPSG reaches $1 0 ^ { - 5 }$ in a few seconds, each step-size refresh costing only two scalar accumulations and no line search. Figure 4 (c) explains what tuning would buy the baselines and why adaptivity still wins in practice: the final gap of Ada-BPSG is essentially flat across the entire initial-step grid, whereas fixed-step SAGA is sharply V-shaped—it can reach a much lower gap, but only in a narrow band around a single step several orders of magnitude above the guaranteed cap, and it diverges beyond it—while SGD is pinned at its variance floor. Ada-BPSG thus recovers the accuracy of a hand-tuned step without any tuning and without the attendant fragility. Together Figure 4 exhibits the predicted $O ( n / K )$ decay and the practical value of adapting past the worst-case safeguard, complementing the harder out-of-theory test that follows.

![](images/aa4a935fa5b655ebf846859f2bf9a78401051cb2879e86bb0bf3c8c1c8a64c9a.jpg)  
(a) objective gap vs. efective passes

![](images/d9a639854abe662880e5085c57a5111db8f49641d04ed28e0ba1f0e156ebe94c.jpg)  
(b) objective gap vs. wall-clock time

![](images/165aeceadcc77dfd5b0ac3fa2ae7a69badfc14db5c283b3d393317bcb6d0024b.jpg)  
(c) sensitivity to the initial step  
Figure 4: Simplex-constrained Poisson inverse problem under the entropy kernel. (a) Objective gap $F ( x _ { K } ) - F ^ { \star }$ against efective passes, with the $O ( n / K )$ reference slope of Theorem 3.6 overlaid; each fixed-step baseline uses the a-priori step guaranteed by its own analysis, and “Ada-BPSG (theory cap)” is the adaptive rule confined to the worst-case safeguard, whereas “Ada-BPSG $( \mathrm { o u r s } ) ^ { \mathfrak { N } }$ relaxes the cap to the local component constant. (b) The same gap against wall-clock time. (c) Final objective gap after a fixed budget as the initial step sweeps the grid $\lbrace \bar { 1 0 } ^ { - 3 } , \dots , 1 0 ^ { 2 } \rbrace$ : for the fixed-step methods the swept value is the step, while for Ada-BPSG it is only the starting point of the adaptive rule. Lines and shaded regions show the mean and standard deviation over five runs at batch size $b = 8 .$

## 4.3 Real-data confirmation: hyperspectral unmixing

The synthetic study isolates the mechanism on controlled data; we now verify that it survives on measured observations, with the same estimator, safeguards, and hyperparameters. We use the Samson scene [49], $9 5 \times 9 5$ pixels over $L = 1 5 6$ contiguous bands with three reference materials (soil, tree, water). To assemble a large finite sum on the simplex, we pool a spatially contiguous, spectrally homogeneous patch R under a shared-abundance model: all pixels in R are taken to share one abundance $x \in \Delta _ { d }$ over a dictionary $D \in \mathbb { R } _ { + } ^ { L \times d }$ of measured spectra. Each material contributes an endmember bundle of $K = 6$ representative spectra—cluster centers of the pixels it dominates, the standard construction for spectral variability [39]—so $d = 1 8$ , and summing the weights within a bundle recovers a physical material abundance. Every pixel–band pair $( p , \ell )$ with $p \in R$ is one component, with atom $a _ { i } = D _ { \ell , : } \geq 0$ and count $b _ { i } = Y _ { p \ell } \geq 0$ the measured radiance, giving

$$
\operatorname* { m i n } _ { x \in \Delta } \ { \frac { 1 } { n } } \sum _ { i = 1 } ^ { n } { \Big ( } \langle a _ { i } , x \rangle - b _ { i } \log \langle a _ { i } , x \rangle { \Big ) } , \qquad n = | R | \cdot L ,
$$

the same generalized Kullback–Leibler (Poisson) fidelity as (4.1), now with real atoms and radiances. We select the $1 0 \times 1 0$ patch that is simultaneously most mixed and most homogeneous— mean abundance (0.37, 0.53, 0.10), a genuinely interior target—which yields $n = 1 5 6 0 0$ . Exactly as in the synthetic case, the worst-case component constant is inflated by measured reflectances that dip to $p \approx 8 . 6 \times 1 0 ^ { - 3 }$ in some bands: here $\bar { L } \approx 6 8$ and $M \approx 5 . 6 \times 1 0 ^ { 3 }$ , whereas the local component smoothness is only $M _ { \mathrm { l o c } } \approx 2 3$ . We keep the batch size $b = 8$ , the a-priori baseline protocol, and the two Ada-BPSG safeguards of Section 4.2 unchanged, and again obtain $F ^ { \star }$ from an independent full-batch mirror-descent solver.

Figure 5 reproduces the synthetic behavior on real data. Held to the steps their own analysis follows the worst-case M, the fixed baselines stall between $1 0 ^ { - 2 }$ and $1 0 ^ { - 3 } \ ( \mathrm { B P G \ 8 . 8 \times 1 0 ^ { - 3 } }$ , SGD $3 . 6 \times 1 0 ^ { - 3 }$ , SAGA 7.1 × 10<sup>−3</sup>, SARAH $1 . 4 \times 1 0 ^ { - 3 } )$ , and the “theory $\mathrm { c a p } ^ { \mathrm { , } \mathrm { , } }$ run of Ada-BPSG sits among them at $7 . 1 \times 1 0 ^ { - 3 } ;$ the analysis-faithful step is once more conservative on the instance. Relaxing the safeguard to the local constant, Ada-BPSG drives the gap to $2 . 2 \times 1 0 ^ { - 6 }$ —more than two orders of magnitude below the best a-priori baseline—tracking the $O ( n / K )$ reference slope and reaching $1 0 ^ { - 5 }$ in a few seconds of wall-clock (Figure 5 (b)). Figure 5 (c) is transparent about the role of tuning: with its step hand-swept, fixed SAGA reaches near machine precision over a broad band of large steps, below Ada-BPSG, so on this instance a well-chosen constant step is very forgiving once found; the adaptive rule instead stays flat at $\approx 2 \times 1 0 ^ { - 6 }$ across five orders of magnitude of initial step, recovering a low gap with neither a per-instance search nor a line search, from the SAGA information it already maintains. The mechanism the synthetic experiment isolates thus governs a real unmixing problem under the identical configuration.

## 4.4 Sparse nonnegative matrix factorization

The third experiment tests the non-Euclidean update on sparse nonnegative matrix factorization (NMF). Given $\mathcal { M } \in \mathbb { R } ^ { I _ { 1 } \times I _ { 2 } }$ and a target rank $r <$ min $\{ I _ { 1 } , I _ { 2 } \}$ , we solve

$$
\operatorname* { m i n } _ { U , V } \quad \Phi ( U , V ) : = \frac { 1 } { 2 } \| \mathcal { M } - U V \| _ { F } ^ { 2 } ,
$$

$$
\begin{array} { r l r } { \mathrm { s u b j e c t ~ t o } } & { U , V \geqslant 0 , } \\ & { \| U _ { : j } \| _ { 0 } \leqslant s _ { 1 } , } & { \| V _ { j : } \| _ { 0 } \leqslant s _ { 2 } \quad ( j \in [ r ] ) . } \end{array}\tag{4.2}
$$

where $U \in \mathbb { R } _ { + } ^ { I _ { 1 } \times r } , V \in \mathbb { R } _ { + } ^ { r \times I _ { 2 } } , U _ { : j }$ is column $j$ of $U , V _ { j } { \mathrm { : } }$ is row $j$ of $V .$ , and $\| \cdot \| _ { 0 }$ counts nonzero entries. Equivalently, the constraints form the extended-valued term $h ,$ whose indicator is zero on the feasible set and +∞ outside it.

We use the quartic kernel

$$
\psi ( U , V ) = 3 \psi _ { 1 } ( U , V ) + \| \mathcal { M } \| _ { F } \psi _ { 2 } ( U , V ) ,
$$

with

$$
\psi _ { 1 } ( U , V ) : = \bigg ( \frac { \| U \| _ { F } ^ { 2 } + \| V \| _ { F } ^ { 2 } } { 2 } \bigg ) ^ { 2 } , \qquad \psi _ { 2 } ( U , V ) : = \frac { \| U \| _ { F } ^ { 2 } + \| V \| _ { F } ^ { 2 } } { 2 } ,
$$

which matches the relative-smooth geometry of matrix factorization [32]. Prior results permit the relative-smoothness scale $\bar { L } \geqslant 1$ for this kernel [8, 43]; we set $\bar { L } = 1$ . Because the cardinality constraints make h nonconvex, this experiment evaluates the algorithmic mechanism beyond the convex-regularizer setting of Assumption 3.4.

On the ORL dataset of 400 normalized face images, we set $r \ = \ 2 5$ , sample 5% of the components per iteration, and test four sparsity levels. We compare Ada-BPSG with BPG and with BPSG/BPSGE using SGD, SAGA, or SARAH estimators. BPSGE is the extrapolated linesearch variant. The smoothness scale M is estimated by the power method used in Wang et al. [43], and the initial step size is selected from the same grid as in the classification experiments. Each method runs for 200 epochs.

![](images/1fd17a7bae849811a6a8064bf954b1a92b8289cd6a58b344e4e2b7f4aa4f50eb.jpg)  
(a) objective gap vs. efective passes

![](images/ec59653abbd7ea104154b8f0aa671e21abcb505f12138101720b866e7b93d4c9.jpg)  
(b) objective gap vs. wall-clock time

![](images/263da551aa160dedbe25f97939f7358f96792f49e869b74e77d65d2b388f6dac.jpg)  
(c) sensitivity to the initial step

Figure 5: Region-pooled Poisson unmixing on the Samson hyperspectral scene, read along the same axes as Figure 4. Fixed-step baselines use their a-priori guaranteed steps; “Ada-BPSG (theory cap)” is confined to the worst-case safeguard, “Ada-BPSG (ours)” relaxes it to the local component constant. (c) sweeps the initial step—the step itself for the fixed methods, only the starting point for Ada-BPSG. Mean and standard deviation over five runs at $b = 8 .$  
![](images/6ad5702c00c7c760841f68908b6b4a2a171e147d3b3759e10e99c2a5d9325a58.jpg)  
(a) $s _ { 1 } = I _ { 1 } / 2$ and s<sub>2</sub> =  
I<sub>2</sub>/2

![](images/cd001ca84dbe80c23905ba2f2d4be0d96ca2407eae33f33e718422f661227914.jpg)  
I<sub>2</sub>/4  
(b) s<sub>1</sub> = I<sub>1</sub>/4 and s<sub>2</sub> =

![](images/455dde74cb6429238b20ccb6b1ae96a2b5b1c39e3ea52895cbc6bec35b3f7cc7.jpg)  
(c) s<sub>1</sub> = I<sub>1</sub>/6 and s<sub>2</sub> =  
I<sub>2</sub>/6

![](images/e98f3c459dcf1fb698d7c23e9495256ca0017e9c5ccebaf30ce84fda83f60784.jpg)  
(d) $s _ { 1 } = I _ { 1 } / 8$ and s<sub>2</sub> =  
I<sub>2</sub>/8  
Figure 6: Sparse NMF on ORL under four cardinality levels. Ada-BPSG attains the lowest objective trajectory in each regime, including against the extrapolated BPSGE methods that use line search.

Figure 6 completes the empirical chain. Ada-BPSG descends faster than the stochastic BPSG variants and remains slightly below the two extrapolated BPSGE curves throughout the four sparsity regimes. Together with the Euclidean results, this shows that the stabilized adaptive step transfers from ordinary proximal SAGA to a problem-adapted Bregman geometry without introducing line-search evaluations.

## 5 Conclusion

Ada-BPSG turns the gradient information already maintained by SAGA into a stable, curvatureadaptive Bregman proximal step. Its mediant aggregation downweights unreliable local BB ratios, while clipping and monotonicity supply the deterministic invariant needed for convergence. This separation keeps the method line-search-free and makes the algorithmic and theoretical roles of the step-size components explicit.

The analysis follows a single chain across regimes: relative smoothness yields Bregman descent, local component smoothness controls the SAGA table error, and the safeguard balances the two. The resulting guarantees comprise an $O ( n / K )$ convex rate, a restarted linear rate under relative quadratic growth, and an $O ( 1 / K )$ bound on the expected squared proximal residual in the nonconvex setting. The experiments reflect the same mechanism. Ada-BPSG improves gradient eficiency and initial-step robustness in Euclidean logistic regression, exhibits the predicted convex rate on a simplex-constrained Poisson inverse problem under the entropy kernel—a non-Euclidean instance meeting every hypothesis of the analysis, where it adapts past the conservative worst-case safeguard to outrun every fixed-step baseline held to its guaranteed step, with the same advantage confirmed on real hyperspectral unmixing data—and achieves the lowest objective trajectories among the compared Bregman methods for sparse NMF, without the line search used by the extrapolated baselines.

Limitations and directions. The current theory analyzes the clipped sequence on a deterministic bounded region with a strongly convex kernel; the admissible safeguard depends on L<sup>¯</sup> and the local component constant M. General finite-sum models also retain the O(nd) SAGA table, although linear-prediction losses admit the O(n) implementation in Appendix A.2. Extending the same stabilized candidate to memory-light SVRG, SARAH, or SPIDER estimators, and deriving safeguards under weaker kernel and localization conditions, are natural next steps.

## References

[1] Kimon Antonakopoulos and Panayotis Mertikopoulos. Adaptive first-order methods revisited: Convex optimization without lipschitz requirements. In Advances in Neural Information Processing Systems, volume 34, 2021.

[2] Kimon Antonakopoulos, E. Veronica Belmega, and Panayotis Mertikopoulos. An adaptive mirror-prox method for variational inequalities with singular operators. In Advances in Neural Information Processing Systems, volume 32, 2019.

[3] Kimon Antonakopoulos, Dong Quan Vu, Volkan Cevher, Kfir Y. Levy, and Panayotis Mertikopoulos. A universal black-box optimization method with almost dimension-free convergence rate guarantees. In Proceedings of the 39th International Conference on Machine Learning, volume 162 of Proceedings of Machine Learning Research, pages 694–726, 2022.

[4] Francis Bach and Kfir Y. Levy. A universal algorithm for variational inequalities adaptive to smoothness and noise. In Proceedings of the Thirty-Second Conference on Learning Theory, volume 99 of Proceedings of Machine Learning Research, pages 164–194. PMLR, 2019.

[5] Jonathan Barzilai and Jonathan M Borwein. Two-point step size gradient methods. IMA journal of numerical analysis, 8(1):141–148, 1988.

[6] Heinz H. Bauschke and Jonathan M. Borwein. Legendre functions and the method of random bregman projections. Journal of Convex Analysis, 4(1):27–67, 1997.

[7] Heinz H Bauschke, J´erˆome Bolte, and Marc Teboulle. A descent lemma beyond lipschitz gradient continuity: first-order methods revisited and applications. Mathematics of Operations Research, 42(2):330–348, 2017.

[8] J´erˆome Bolte, Shoham Sabach, Marc Teboulle, and Yakov Vaisbourd. First order methods beyond convexity and lipschitz gradient continuity with applications to quadratic inverse problems. SIAM Journal on Optimization, 28(3):2131–2151, 2018.

[9] Minh N. Bui and Patrick L. Combettes. Bregman forward-backward operator splitting. Set-Valued and Variational Analysis, 29(3):583–603, 2021.

[10] Oleg Burdakov, Yu-Hong Dai, and Na Huang. Stabilized barzilai-borwein method. arXiv preprint arXiv:1907.06409, 2019.

[11] Dan Butnariu and Alfredo N. Iusem. Totally Convex Functions for Fixed Points Computation and Infinite Dimensional Optimization. Kluwer Academic Publishers, 2000.

[12] Chih-Chung Chang and Chih-Jen Lin. Libsvm: a library for support vector machines. ACM transactions on intelligent systems and technology (TIST), 2(3):1–27, 2011.

[13] Michael B. Cohen, Aaron Sidford, and Kevin Tian. Relative lipschitzness in extragradient methods and a direct recipe for acceleration. In 12th Innovations in Theoretical Computer Science Conference, volume 185 of LIPIcs, pages 62:1–62:18, 2021. doi: 10.4230/LIPIcs. ITCS.2021.62.

[14] Pierre Comon, Gene Golub, Lek-Heng Lim, and Bernard Mourrain. Symmetric tensors and symmetric tensor rank. SIAM Journal on Matrix Analysis and Applications, 30(3): 1254–1279, 2008.

[15] Aaron Defazio, Francis R. Bach, and Simon Lacoste-Julien. SAGA: A fast incremental gradient method with support for non-strongly convex composite objectives. In Advances in Neural Information Processing Systems, pages 1646–1654, 2014.

[16] Benjamin Dubois-Taine, Sharan Vaswani, Reza Babanezhad, Mark Schmidt, and Simon Lacoste-Julien. SVRG meets adagrad: Painless variance reduction. CoRR, abs/2102.09645, 2021.

[17] John C. Duchi, Elad Hazan, and Yoram Singer. Adaptive subgradient methods for online learning and stochastic optimization. J. Mach. Learn. Res., 12:2121–2159, 2011. URL http://dl.acm.org/citation.cfm?id=2021068.

[18] Roger Fletcher. On the barzilai-borwein method. In Optimization and control with applications, pages 235–256. Springer, 2005.

[19] Filip Hanzely, Peter Richt´arik, and Lin Xiao. Accelerated bregman proximal gradient methods for relatively smooth convex optimization. Computational Optimization and Applications, 79(2):405–440, 2021. doi: 10.1007/s10589-021-00273-8.

[20] Marzieh Hasannasab, Johannes Hertrich, Sebastian Neumayer, Gerlind Plonka, Simon Setzer, and Gabriele Steidl. Parseval proximal neural networks. Journal of Fourier Analysis and Applications, 26:1–31, 2020.

[21] Feihu Huang and Yuning Luo. Faster double adaptive gradient methods. In Proceedings of the AAAI Conference on Artificial Intelligence, volume 39, pages 27018–27026, 2025.

[22] Feihu Huang, Xidong Wu, and Heng Huang. Eficient mirror descent ascent methods for nonsmooth minimax problems. Advances in Neural Information Processing Systems, 34: 10431–10443, 2021.

[23] Rie Johnson and Tong Zhang. Accelerating stochastic gradient descent using predictive variance reduction. In Advances in Neural Information Processing Systems, pages 315–323, 2013.

[24] Ali Kavis, Kfir Y. Levy, Francis Bach, and Volkan Cevher. UniXGrad: A universal, adaptive algorithm with optimal guarantees for constrained optimization. In Advances in Neural Information Processing Systems, volume 32, 2019.

[25] Ali Kavis, Stratis Skoulakis, Kimon Antonakopoulos, Leello Tadesse Dadi, and Volkan Cevher. Adaptive stochastic variance reduction for non-convex finite-sum minimization. Advances in Neural Information Processing Systems, 35:23524–23538, 2022.

[26] Diederik P. Kingma and Jimmy Ba. Adam: A method for stochastic optimization. In Yoshua Bengio and Yann LeCun, editors, 3rd International Conference on Learning Representations, ICLR 2015, San Diego, CA, USA, May 7-9, 2015, Conference Track Proceedings, 2015. URL http://arxiv.org/abs/1412.6980.

[27] Sen Kuang, Benwei Wu, and Shuang Cong. A fusion measurement approach to improve quantum state tomography eficiency and accuracy. IEEE Transactions on Instrumentation and Measurement, 69(6):3049–3060, 2019.

[28] Kfir Y. Levy, Alp Yurtsever, and Volkan Cevher. Online adaptive methods, universality and acceleration. In Advances in Neural Information Processing Systems, volume 31, 2018.

[29] Haihao Lu, Robert M. Freund, and Yurii Nesterov. Relatively smooth convex optimization by first-order methods, and applications. SIAM Journal on Optimization, 28(1):333–354, 2018. doi: 10.1137/16M1099546.

[30] Ke Ma, Jinshan Zeng, Jiechao Xiong, Qianqian Xu, Xiaochun Cao, Wei Liu, and Yuan Yao. Stochastic non-convex ordinal embedding with stabilized barzilai-borwein step size. In Proceedings of the AAAI Conference on Artificial Intelligence, volume 32, 2018.

[31] Yura Malitsky and Konstantin Mishchenko. Adaptive proximal gradient method for convex optimization. Advances in Neural Information Processing Systems, 37:100670–100697, 2024.

[32] Mahesh Chandra Mukkamala and Peter Ochs. Beyond alternating updates for matrix factorization with inertial bregman proximal gradient algorithms. Advances in Neural Information Processing Systems, 32, 2019.

[33] Lam M. Nguyen, Jie Liu, Katya Scheinberg, and Martin Tak´ac. SARAH: A novel method for machine learning problems using stochastic recursive gradient. In Proceedings of the 34th International Conference on Machine Learning, 2017.

[34] Van Dung Nguyen and B\`ang Cˆong V˜u. A stochastic variance reduction algorithm with bregman distances for structured composite problems. Optimization, 72(6):1463–1484, 2023. doi: 10.1080/02331934.2022.2027413.

[35] Boris T Polyak. Some methods of speeding up the convergence of iteration methods. Ussr computational mathematics and mathematical physics, 4(5):1–17, 1964.

[36] Nicolas Le Roux, Mark Schmidt, and Francis R. Bach. A stochastic gradient method with an exponential convergence rate for finite training sets. In Advances in Neural Information Processing Systems, pages 2672–2680, 2012.

[37] Zheng Shi, Nicolas Loizou, Peter Richt´arik, and Martin Tak´ac. AI-SARAH: adaptive and implicit stochastic recursive gradient methods. CoRR, abs/2102.09700, 2021.

[38] Antonio Silveti-Falls, Cesare Molinari, and Jalal Fadili. A stochastic bregman primal-dual splitting algorithm for composite optimization. arXiv preprint arXiv:2112.11928, 2021.

[39] Ben Somers, Maciel Zortea, Antonio Plaza, and Gregory P. Asner. Automated extraction of image-based endmember bundles for improved spectral unmixing. IEEE Journal of Selected Topics in Applied Earth Observations and Remote Sensing, 5(2):396–408, 2012. doi: 10.1109/JSTARS.2011.2181340.

[40] Conghui Tan, Shiqian Ma, Yu-Hong Dai, and Yuqiu Qian. Barzilai-borwein step size for stochastic gradient descent. In Advances in Neural Information Processing Systems, pages 685–693, 2016.

[41] Quang Van Nguyen. Forward-backward splitting with bregman distances. Vietnam Journal of Mathematics, 45(3):519–539, 2017.

[42] Qingsong Wang and Deren Han. A bregman stochastic method for nonconvex nonsmooth problem beyond global lipschitz gradient continuity. Optimization Methods and Software, 38(5):914–946, 2023.

[43] Qingsong Wang, Zehui Liu, Chunfeng Cui, and Deren Han. A bregman proximal stochastic gradient method with extrapolation for nonconvex nonsmooth problems. In Proceedings of the AAAI Conference on Artificial Intelligence, volume 38, pages 15580–15588, 2024.

[44] Qingsong Wang, Shengze Xu, Xiaojiao Tong, and Tieyong Zeng. An improved optimal proximal gradient algorithm for non-blind image deblurring. arXiv preprint arXiv:2502.07602, 2025.

[45] Rachel Ward, Xiaoxia Wu, and L´eon Bottou. Adagrad stepsizes: sharp convergence over nonconvex landscapes. In Kamalika Chaudhuri and Ruslan Salakhutdinov, editors, Proceedings of the 36th International Conference on Machine Learning, ICML 2019, 9-15 June 2019, Long Beach, California, USA, volume 97 of Proceedings of Machine Learning Research, pages 6677–6686. PMLR, 2019. URL http://proceedings.mlr.press/v97/ ward19a.html.

[46] Zhuang Yang. Improved powered stochastic optimization algorithms for large-scale machine learning. Journal of Machine Learning Research, 24(241):1–29, 2023.

[47] Danqing Zhou, Shiqian Ma, and Junfeng Yang. Adabb: Adaptive barzilai-borwein method for convex optimization. arXiv preprint arXiv:2401.08024, 2024.

[48] Kaiwen Zhou, Qinghua Ding, Fanhua Shang, James Cheng, Danli Li, and Zhi-Quan Luo. Direct acceleration of saga using sampled negative momentum. In The 22nd International Conference on Artificial Intelligence and Statistics, pages 1602–1610, 2019.

[49] Feiyun Zhu. Hyperspectral unmixing: Ground truth labeling, datasets, benchmark performances and survey. arXiv preprint arXiv:1708.05125, 2017.

## A Proofs and Supplementary Details

## A.1 Auxiliary inequalities

Lemma A.1 (Three-point proximal inequality). Let

$$
x ^ { + } = \arg \operatorname* { m i n } _ { x } \left\{ h ( x ) + \langle g , x \rangle + \frac { 1 } { \eta } D _ { \psi } ( x , x _ { k } ) \right\} .
$$

Then, for any $u \in \mathbf { E }$

$$
h ( x ^ { + } ) - h ( u ) + \langle g , x ^ { + } - u \rangle \leqslant \frac { 1 } { \eta } \left[ D _ { \psi } ( u , x _ { k } ) - D _ { \psi } ( u , x ^ { + } ) - D _ { \psi } ( x ^ { + } , x _ { k } ) \right] .
$$

Proof. The optimality condition gives

$$
0 \in \partial h ( x ^ { + } ) + g + \frac { 1 } { \eta } ( \nabla \psi ( x ^ { + } ) - \nabla \psi ( x _ { k } ) ) .
$$

Choose $\xi ^ { + } \in \partial h ( x ^ { + } )$ satisfying this inclusion. By convexity of $h$

$$
h ( u ) \geqslant h ( x ^ { + } ) + \langle \xi ^ { + } , u - x ^ { + } \rangle .
$$

Substituting $\xi ^ { + } = - g - \eta ^ { - 1 } ( \nabla \psi ( x ^ { + } ) - \nabla \psi ( x _ { k } ) )$ and using the Bregman three-point identity gives the result. □

## A.2 Scalar-table implementation

For a linear-prediction loss $f _ { i } ( x ) = \Psi _ { i } ( \langle a _ { i } , x \rangle )$ , the scalar $\Phi _ { i } ^ { k } : = \langle a _ { i } , \varphi _ { i } ^ { k } \rangle$ determines the stored component gradient. Algorithm 2 maintains these scalars together with the aggregate gradient $\mu _ { k }$ , reducing SAGA table storage from $O ( n d )$ to $O ( n )$ [48]. The two secant accumulators remain scalar, so the adaptive step requires no additional vector table.

## A.3 Proof of Lemma 3.5

Let

$$
a _ { i } ^ { k } : = \nabla f _ { i } ( x _ { k } ) - \nabla f _ { i } ( \varphi _ { i } ^ { k } ) , \qquad { \bar { a } } ^ { k } : = { \frac { 1 } { n } } \sum _ { i = 1 } ^ { n } a _ { i } ^ { k } .
$$

Since $\begin{array} { r } { \mu _ { k } = \frac { 1 } { n } \sum _ { i } \nabla f _ { i } ( \varphi _ { i } ^ { k } ) } \end{array}$ , the estimator can be written as

$$
\widetilde { \nabla } _ { k } = \frac { 1 } { b } \sum _ { i \in B _ { k } } a _ { i } ^ { k } + \mu _ { k } .
$$

The uniform sampling assumption gives

$$
\mathbb { E } _ { k } [ \widetilde { \nabla } _ { k } ] = \bar { a } ^ { k } + \mu _ { k } = \nabla f ( x _ { k } ) .
$$

Moreover,

$$
\widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) = \frac { 1 } { b } \sum _ { i \in B _ { k } } ( a _ { i } ^ { k } - \bar { a } ^ { k } ) .
$$

$\mathrm { B y }$ the sampling second-moment bound in Assumption 3.4,

$$
\mathbb { E } _ { k } \| \widetilde { \nabla } _ { k } - \nabla f ( { x } _ { k } ) \| _ { * } ^ { 2 } \leqslant \frac { \sigma _ { b } } { b n } \sum _ { i = 1 } ^ { n } \| a _ { i } ^ { k } - { \bar { a } } ^ { k } \| _ { * } ^ { 2 }
$$

$$
\leqslant \frac { 4 \sigma _ { b } } { b n } \sum _ { i = 1 } ^ { n } \| a _ { i } ^ { k } \| _ { * } ^ { 2 } .
$$

Algorithm 2 Eficient Implementation of Ada-BPSG   
1: Input: max iterations $K ,$ batch-size $b \in \{ 1 , \ldots , n \}$ , initial point $x _ { 0 }$   
initial step-size $\eta _ { 0 } \in [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ] .$ update frequency m $\geqslant 1$   
BB scaling $\alpha > 0 .$ , BB bufer $\theta > 0 .$ , safeguard bounds $0 < \eta _ { \mathrm { m i n } } \leqslant$ η<sub>max</sub>   
2: Initialize: scalar table $\Phi _ { i } ^ { 0 } = \langle a _ { i } , x _ { 0 } \rangle$ for $i = 1 , \ldots , n$   
$\begin{array} { r } { \mu _ { 0 } = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \nabla \Psi _ { i } ( \Phi _ { i } ^ { 0 } ) a _ { i } , \dot { \xi } _ { 0 } = 0 , } \end{array}$ and $\delta _ { 0 } = 0$   
3: for $k = 0 , \ddot { 1 } , \dotsc , K - 1$ do   
4: if $k > 0$ then   
5: $\eta _ { k } = \eta _ { k - 1 }$   
6: end if   
7: if $k > 0$ and $k$ mod $m = 0$ then   
8: $\widehat { \eta } _ { k } = \bigg \{ \frac { 1 } { \alpha } \frac { \zeta k } { \delta _ { k } + \theta \xi _ { k } } , \quad \xi _ { k } > 0 ,$   
<sub></sub>η<sub>k−1</sub>, $\xi _ { k } = 0 ,$   
9: $\eta _ { k } = \operatorname* { m a x } \{ \eta _ { k - 1 } , \mathrm { c l i p } _ { [ \eta _ { \mathrm { m i n } } , \eta _ { \mathrm { m a x } } ] } ( \widehat { \eta } _ { k } ) \}$   
10: $\xi _ { k } = 0 , \delta _ { k } = 0$   
11: end if   
12: Randomly pick a batch $B _ { k }$ uniformly without replacement.   
13: Store $\Phi _ { i } ^ { k + 1 } = \langle a _ { i } , x _ { k } \rangle$ for $i \in B _ { k }$   
14: Set $\Phi _ { i } ^ { k + \mathbf { \bar { 1 } } } = \Phi _ { i } ^ { k }$ for $i \notin B _ { k }$   
15: $\begin{array} { r } { \widetilde { \nabla } _ { k } = \frac { 1 } { b } \sum _ { i \in B _ { k } } \left( \nabla \Psi _ { i } ( \Phi _ { i } ^ { k + 1 } ) - \nabla \Psi _ { i } ( \Phi _ { i } ^ { k } ) \right) a _ { i } + \mu _ { k } } \end{array}$   
16: $\begin{array} { r } { x _ { k + 1 } = \arg \operatorname* { m i n } _ { x } \Big \{ h ( x ) + \langle \widetilde { \nabla } _ { k } , x \rangle + \frac { 1 } { \eta _ { k } } D _ { \psi } ( x , x _ { k } ) \Big \} } \end{array}$   
17: $\begin{array} { r } { \xi _ { k + 1 } = \xi _ { k } + \sum _ { i \in B _ { k } } \big | \big ( \Phi _ { i } ^ { k + 1 } - \Phi _ { i } ^ { k } \big ) \big ( \nabla \Psi _ { i } ( \Phi _ { i } ^ { k + 1 } ) - \nabla \Psi _ { i } ( \Phi _ { i } ^ { k } ) \big ) \big | } \end{array}$   
18: $\begin{array} { r l } & { \delta _ { k + 1 } = \delta _ { k } + \sum _ { i \in B _ { k } } \left( \left( \nabla \Psi _ { i } ( \Phi _ { i } ^ { k + 1 } ) - \nabla \Psi _ { i } ( \Phi _ { i } ^ { k } ) \right) ^ { 2 } \cdot \| a _ { i } \| _ { * } ^ { 2 } \right) } \end{array}$   
19: $\begin{array} { r } { \mu _ { k + 1 } = \mu _ { k } + \frac { 1 } { n } \sum _ { i \in B _ { k } } \left( \nabla \Psi _ { i } ( \Phi _ { i } ^ { k + 1 } ) - \nabla \Psi _ { i } ( \Phi _ { i } ^ { k } ) \right) a _ { i } } \end{array}$   
20: end for   
21: return $x _ { K }$

For the convex setting, the self-bounding condition implies

$$
\begin{array} { r l } & { \| a _ { i } ^ { k } \| _ { * } ^ { 2 } \leqslant 2 \| \nabla f _ { i } ( x _ { k } ) - \nabla f _ { i } ( x ^ { * } ) \| _ { * } ^ { 2 } + 2 \| \nabla f _ { i } ( \varphi _ { i } ^ { k } ) - \nabla f _ { i } ( x ^ { * } ) \| _ { * } ^ { 2 } } \\ & { \qquad \leqslant 4 M \left( \Delta _ { i } ( x _ { k } ) + \Delta _ { i } ( \varphi _ { i } ^ { k } ) \right) . } \end{array}
$$

Combining the two displays yields

$$
\mathbb { E } _ { k } \Vert \widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) \Vert _ { * } ^ { 2 } \leqslant \frac { 1 6 \sigma _ { b } M } { b } \left[ \Delta ( x _ { k } ) + \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \Delta _ { i } ( \varphi _ { i } ^ { k } ) \right] .
$$

□

## A.4 Proof of Theorem 3.6

Let

$$
Q _ { i } ( x ) : = \Delta _ { i } ( x ) + H ( x ) , \qquad T _ { k } : = \frac { 1 } { \omega } \frac { 1 } { n } \sum _ { i = 1 } ^ { n } Q _ { i } ( \varphi _ { i } ^ { k } ) , \qquad \omega : = \frac { 4 b } { n } .
$$

Since $\varphi _ { i } ^ { 0 } = x _ { 0 }$ and $\nabla f ( x ^ { * } ) + \rho ^ { * } = 0 .$

$$
T _ { 0 } = \frac { n } { 4 b } \left( F ( x _ { 0 } ) - F ( x ^ { * } ) \right) .
$$

The table update $\mathrm { g i }$ ves

$$
\mathbb { E } _ { k } [ T _ { k + 1 } ] = \left( 1 - { \frac { b } { n } } \right) T _ { k } + { \frac { b } { n \omega } } \left( F ( x _ { k } ) - F ( x ^ { * } ) \right) .
$$

Using the upper smooth-adaptability of $f$ and Lemma $\mathrm { A . 1 }$ with $u = x ^ { * }$ and $g = \widetilde { \nabla } _ { k }$ , we first obtain

$$
\begin{array} { r l } & { F ( x _ { k + 1 } ) - F ( x ^ { * } ) \leqslant f ( x _ { k } ) - f ( x ^ { * } ) - \langle \nabla f ( x _ { k } ) , x _ { k } - x ^ { * } \rangle } \\ & { \qquad + \langle \nabla f ( x _ { k } ) - \widetilde { \nabla } _ { k } , x _ { k + 1 } - x _ { k } \rangle + \langle \nabla f ( x _ { k } ) - \widetilde { \nabla } _ { k } , x _ { k } - x ^ { * } \rangle } \\ & { \qquad + \left( \hat { L } - \displaystyle \frac { 1 } { \eta _ { k } } \right) D _ { \psi } ( x _ { k + 1 } , x _ { k } ) + \displaystyle \frac { 1 } { \eta _ { k } } \left( D _ { \psi } ( x ^ { * } , x _ { k } ) - D _ { \psi } ( x ^ { * } , x _ { k + 1 } ) \right) . } \end{array}
$$

By convexity of $f ,$ the first line is nonpositive. Taking conditional expectation and using $\mathbb { E } _ { k } [ \widetilde { \nabla } _ { k } ] = \nabla f ( x _ { k } )$ gives

$$
\begin{array} { r l } & { \mathbb { E } _ { k } \big [ F ( x _ { k + 1 } ) - F ( x ^ { * } ) \big ] \leqslant \mathbb { E } _ { k } \langle \nabla f ( x _ { k } ) - \widetilde { \nabla } _ { k } , x _ { k + 1 } - x _ { k } \rangle } \\ & { \qquad + \left( \hat { L } - \displaystyle \frac { 1 } { \eta _ { k } } \right) \mathbb { E } _ { k } D _ { \psi } ( x _ { k + 1 } , x _ { k } ) + \displaystyle \frac { 1 } { \eta _ { k } } \left( D _ { \psi } ( x ^ { * } , x _ { k } ) - \mathbb { E } _ { k } D _ { \psi } ( x ^ { * } , x _ { k + 1 } ) \right) . } \end{array}
$$

By Young’s inequality and $\begin{array} { r } { D _ { \psi } ( x _ { k + 1 } , x _ { k } ) \geqslant \frac 1 2 \| x _ { k + 1 } - x _ { k } \| ^ { 2 } . } \end{array}$

$$
\mathbb { E } _ { k } \langle \nabla f ( x _ { k } ) - \widetilde { \nabla } _ { k } , x _ { k + 1 } - x _ { k } \rangle \leqslant \frac { 1 } { 2 \beta } \mathbb { E } _ { k } \| \widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) \| _ { * } ^ { 2 } + \beta \mathbb { E } _ { k } D _ { \psi } ( x _ { k + 1 } , x _ { k } ) .
$$

Take $\beta = 3 2 \sigma _ { b } M / b$ . Since $\eta _ { \mathrm { m a x } } \leqslant ( \bar { L } + \beta ) ^ { - 1 }$ , the coeficient of $D _ { \psi } ( x _ { k + 1 } , x _ { k } )$ is nonpositive. Lemma 3.5 gives

$$
\frac { 1 } { 2 \beta } { \mathbb E } _ { k } \| \widetilde \nabla _ { k } - \nabla f ( x _ { k } ) \| _ { * } ^ { 2 } \leqslant \frac { 1 } { 4 } \Delta ( x _ { k } ) + \frac { 1 } { 4 } \frac { 1 } { n } \sum _ { i = 1 } ^ { n } \Delta _ { i } ( \varphi _ { i } ^ { k } ) .
$$

Because $H \geqslant 0$ , we have $\Delta ( x _ { k } ) \leqslant F ( x _ { k } ) - F ( x ^ { * } )$ and $\begin{array} { r } { \frac { 1 } { n } \sum _ { i } \Delta _ { i } ( \varphi _ { i } ^ { k } ) \leqslant \omega T _ { k } } \end{array}$ . Combining these bounds with the recursion for $T _ { k + 1 }$ , and using $b / ( n \omega ) = 1 / 4$ and $\omega / 4 = b / n$ , gives

$$
\begin{array} { r l } & { \mathbb { E } _ { k } [ F ( x _ { k + 1 } ) - F ( x ^ { * } ) + T _ { k + 1 } ] \leqslant T _ { k } + \displaystyle \frac { 1 } { 2 } ( F ( x _ { k } ) - F ( x ^ { * } ) ) } \\ & { \qquad + \displaystyle \frac { 1 } { \eta _ { k } } \left( D _ { \psi } ( x ^ { * } , x _ { k } ) - \mathbb { E } _ { k } D _ { \psi } ( x ^ { * } , x _ { k + 1 } ) \right) . } \end{array}
$$

Rearranging,

$$
\begin{array} { r l } & { \frac { 1 } { 2 } \mathbb { E } _ { k } \bigl [ F ( x _ { k + 1 } ) - F ( x ^ { * } ) \bigr ] \leqslant T _ { k } - \mathbb { E } _ { k } T _ { k + 1 } + \frac { 1 } { 2 } \left( F ( x _ { k } ) - F ( x ^ { * } ) - \mathbb { E } _ { k } \bigl [ F ( x _ { k + 1 } ) - F ( x ^ { * } ) \bigr ] \right) } \\ & { \qquad + \frac { 1 } { \eta _ { k } } \left( D _ { \psi } ( x ^ { * } , x _ { k } ) - \mathbb { E } _ { k } D _ { \psi } ( x ^ { * } , x _ { k + 1 } ) \right) . } \end{array}
$$

Let $a _ { k } = 1 / \eta _ { k }$ . Since the convex theorem assumes $\eta _ { k }$ is nondecreasing, $a _ { k }$ is nonincreasing. Therefore,

$$
\sum _ { k = 0 } ^ { K - 1 } \mathbb { E } \left[ a _ { k } \left( D _ { \psi } ( x ^ { * } , x _ { k } ) - D _ { \psi } ( x ^ { * } , x _ { k + 1 } ) \right) \right] \leqslant a _ { 0 } D _ { \psi } ( x ^ { * } , x _ { 0 } ) \leqslant \frac { 1 } { \eta _ { \operatorname* { m i n } } } D _ { \psi } ( x ^ { * } , x _ { 0 } ) .
$$

Summing the preceding one-step inequality and using $T _ { K } \geqslant 0$ yields

$$
{ \frac { 1 } { 2 } } \sum _ { k = 0 } ^ { K - 1 } \mathbb { E } [ F ( x _ { k + 1 } ) - F ( x ^ { * } ) ] \leqslant T _ { 0 } + { \frac { 1 } { 2 } } ( F ( x _ { 0 } ) - F ( x ^ { * } ) ) + { \frac { 1 } { \eta _ { \operatorname* { m i n } } } } D _ { \psi } ( x ^ { * } , x _ { 0 } ) .
$$

Jensen’s inequality for $\begin{array} { r } { \bar { x } _ { K } = \frac { 1 } { K } \sum _ { k = 1 } ^ { K } x _ { k } } \end{array}$ completes the proof.

## A.5 Proof of Theorem 3.8

Condition on the history up to the beginning of restart round t. The restart round reinitializes the SAGA table by setting all table points to $x ^ { ( t ) }$ and uses a safeguarded step-size sequence satisfying the assumptions of Theorem 3.6 within that round. Therefore the initial Lyapunov term in Theorem 3.6 has the same form with $x _ { 0 }$ replaced by $x ^ { ( t ) }$ . Applying Theorem 3.6 to that round and using the relative quadratic growth condition gives

$$
D _ { \psi } ( x ^ { * } , x ^ { ( t ) } ) \leqslant \frac { 1 } { \mu } \left( F ( x ^ { ( t ) } ) - F ( x ^ { * } ) \right) .
$$

Hence, with $\begin{array} { r } { A _ { b } = \frac { n } { 4 b } + \frac { 1 } { 2 } } \end{array}$

$$
\mathbb { E } \left[ F ( x ^ { ( t + 1 ) } ) - F ( x ^ { * } ) \mid x ^ { ( t ) } \right] \leqslant \frac { 2 } { K } \left( A _ { b } + \frac { 1 } { \mu \eta _ { \mathrm { m i n } } } \right) \left( F ( x ^ { ( t ) } ) - F ( x ^ { * } ) \right) .
$$

Taking total expectation, the stated lower bound on K makes the contraction factor at most $1 / 2$ , and induction gives the claim.

## A.6 Proof of Lemma 3.10

Using the notation from the proof of Lemma 3.5,

$$
\widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) = \frac { 1 } { b } \sum _ { i \in B _ { k } } ( a _ { i } ^ { k } - \bar { a } ^ { k } ) .
$$

The sampling second-moment bound and Jensen’s inequality give

$$
\mathbb { E } _ { k } \| \widetilde { \nabla } _ { k } - \nabla f ( x _ { k } ) \| _ { * } ^ { 2 } \leqslant \frac { 4 \sigma _ { b } } { b n } \sum _ { i = 1 } ^ { n } \| a _ { i } ^ { k } \| _ { * } ^ { 2 } .
$$

By component Lipschitz continuity on the deterministic set $D$

$$
\| a _ { i } ^ { k } \| _ { * } ^ { 2 } = \| \nabla f _ { i } ( x _ { k } ) - \nabla f _ { i } ( \varphi _ { i } ^ { k } ) \| _ { * } ^ { 2 } \leqslant M ^ { 2 } \| x _ { k } - \varphi _ { i } ^ { k } \| ^ { 2 } .
$$

This proves the lemma.

## A.7 Proof of Theorem 3.11

Let

$$
\widetilde { \boldsymbol { x } } _ { k + 1 } = \mathrm { P r o x } _ { h , \eta _ { k } } ^ { \psi } ( \boldsymbol { x } _ { k } , \boldsymbol { \nabla } f ( \boldsymbol { x } _ { k } ) ) .
$$

Let $e _ { k } : = \nabla f ( x _ { k } ) - \widetilde \nabla _ { k }$ . Using upper smooth-adaptability of f and Lemma A.1 for the stochastic prox step with $u = \widetilde { x } _ { k + 1 }$ gives

$$
\begin{array} { r l } & { F ( x _ { k + 1 } ) \leqslant f ( x _ { k } ) + h ( \widetilde x _ { k + 1 } ) + \langle \nabla f ( x _ { k } ) , x _ { k + 1 } - x _ { k } \rangle - \langle \widetilde \nabla _ { k } , x _ { k + 1 } - \widetilde x _ { k + 1 } \rangle } \\ & { \qquad + \left( \hat L - \displaystyle \frac { 1 } { \eta _ { k } } \right) D _ { \psi } ( x _ { k + 1 } , x _ { k } ) + \displaystyle \frac { 1 } { \eta _ { k } } D _ { \psi } ( \widetilde x _ { k + 1 } , x _ { k } ) - \frac { 1 } { \eta _ { k } } D _ { \psi } ( \widetilde x _ { k + 1 } , x _ { k + 1 } ) . } \end{array}
$$

Applying Lemma A.1 to the full-gradient prox step with $u = x _ { k }$ gives

$$
h ( \widetilde x _ { k + 1 } ) - h ( x _ { k } ) + \langle \nabla f ( x _ { k } ) , \widetilde x _ { k + 1 } - x _ { k } \rangle \leqslant - \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde x _ { k + 1 } ) - \frac { 1 } { \eta _ { k } } D _ { \psi } ( \widetilde x _ { k + 1 } , x _ { k } ) .
$$

Combining the last two displays yields

$$
\begin{array} { l } { { \displaystyle F ( x _ { k + 1 } ) \leqslant F ( x _ { k } ) + \langle e _ { k } , x _ { k + 1 } - \widetilde { x } _ { k + 1 } \rangle + \left( \bar { L } - \frac { 1 } { \eta _ { k } } \right) D _ { \psi } ( x _ { k + 1 } , x _ { k } ) } } \\ { { \displaystyle \quad \quad - \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde { x } _ { k + 1 } ) - \frac { 1 } { \eta _ { k } } D _ { \psi } ( \widetilde { x } _ { k + 1 } , x _ { k + 1 } ) . } } \end{array}
$$

Young’s inequality and the strong convexity of $\psi$ imply

$$
\langle e _ { k } , x _ { k + 1 } - \widetilde { x } _ { k + 1 } \rangle \leqslant \frac { \eta _ { k } } { 2 } \| e _ { k } \| _ { * } ^ { 2 } + \frac { 1 } { \eta _ { k } } D _ { \psi } ( \widetilde { x } _ { k + 1 } , x _ { k + 1 } ) .
$$

Taking conditional expectation and applying Lemma 3.10, we obtain

$$
\begin{array} { l } { \displaystyle \mathbb { E } _ { k } [ F ( x _ { k + 1 } ) ] \leqslant F ( x _ { k } ) - \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde { x } _ { k + 1 } ) + \left( \bar { L } - \frac { 1 } { \eta _ { k } } \right) \mathbb { E } _ { k } D _ { \psi } ( x _ { k + 1 } , x _ { k } ) } \\ { \displaystyle \quad \quad + \frac { 2 \sigma _ { b } \eta _ { k } M ^ { 2 } } { b n } \sum _ { i = 1 } ^ { n } \| x _ { k } - \varphi _ { i } ^ { k } \| ^ { 2 } . } \end{array}
$$

Define

$$
S _ { k } : = \frac 1 n \sum _ { i = 1 } ^ { n } \| x _ { k } - \varphi _ { i } ^ { k } \| ^ { 2 } , \qquad p : = \frac { b } { n } ,
$$

and use the deterministic Lyapunov coeficient

$$
c : = \frac { 4 \sigma _ { b } \eta _ { \mathrm { m a x } } M ^ { 2 } n } { b ^ { 2 } } .
$$

Let $d _ { k } : = x _ { k + 1 } - x _ { k }$ and $r _ { i } ^ { k } : = x _ { k } - \varphi _ { i } ^ { k }$ . For a fixed sampled batch $B _ { k }$ , the table update gives

$$
x _ { k + 1 } - \varphi _ { i } ^ { k + 1 } = { \left\{ \begin{array} { l l } { d _ { k } , } & { i \in B _ { k } , } \\ { d _ { k } + r _ { i } ^ { k } , } & { i \notin B _ { k } . } \end{array} \right. }
$$

For any $\gamma > 0$ , Young’s inequality yields

$$
\| d _ { k } + r _ { i } ^ { k } \| ^ { 2 } \leqslant \left( 1 + \frac { 1 } { \gamma } \right) \| d _ { k } \| ^ { 2 } + ( 1 + \gamma ) \| r _ { i } ^ { k } \| ^ { 2 } .
$$

Consequently,

$$
\begin{array} { l } { { \displaystyle S _ { k + 1 } = \frac { 1 } { n } \sum _ { i \in B _ { k } } \| d _ { k } \| ^ { 2 } + \frac { 1 } { n } \sum _ { i \notin B _ { k } } \| d _ { k } + r _ { i } ^ { k } \| ^ { 2 } } } \\ { { \displaystyle \qquad \leqslant \left( 1 + \frac { 1 - p } { \gamma } \right) \| d _ { k } \| ^ { 2 } + \frac { 1 + \gamma } { n } \sum _ { i \notin B _ { k } } \| r _ { i } ^ { k } \| ^ { 2 } } . } \end{array}
$$

Taking conditional expectation and using $D _ { \psi } ( x _ { k + 1 } , x _ { k } ) \geqslant \frac 1 2 \| d _ { k } \| ^ { 2 }$ and $\mathbb { E } _ { k } [ \mathbf { 1 } _ { \left\{ i \notin B _ { k } \right\} } ] = 1 - p ,$ we obtain

$$
\mathbb { E } _ { k } [ S _ { k + 1 } ] \leqslant 2 \left( 1 + \frac { 1 - p } { \gamma } \right) \mathbb { E } _ { k } D _ { \psi } ( x _ { k + 1 } , x _ { k } ) + ( 1 + \gamma ) ( 1 - p ) S _ { k } .\tag{A.1}
$$

Combining (A.1) with the one-step bound for $F ( x _ { k + 1 } )$ gives

$$
\begin{array} { r l } & { \mathbb { E } _ { k } [ F ( x _ { k + 1 } ) + c S _ { k + 1 } ] \leqslant F ( x _ { k } ) - \displaystyle \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde { x } _ { k + 1 } ) } \\ & { \qquad + \left[ \bar { L } - \displaystyle \frac { 1 } { \eta _ { k } } + 2 c \left( 1 + \displaystyle \frac { 1 - p } { \gamma } \right) \right] \mathbb { E } _ { k } D _ { \psi } ( x _ { k + 1 } , x _ { k } ) } \\ & { \qquad + \left[ c ( 1 + \gamma ) ( 1 - p ) + \displaystyle \frac { 2 \sigma _ { b } \eta _ { k } M ^ { 2 } } { b } \right] S _ { k } . } \end{array}
$$

Choose $\gamma = p / 2$ . Since $( 1 + \gamma ) ( 1 - p ) \leqslant 1 - p / 2$ and $\eta _ { k }$ ⩽ $\eta _ { \mathrm { m a x } } .$ , the definition of c gives

$$
c ( 1 + \gamma ) ( 1 - p ) + \frac { 2 \sigma _ { b } \eta _ { k } M ^ { 2 } } { b } \leqslant c .
$$

Moreover,

$$
2 \left( 1 + { \frac { 1 - p } { \gamma } } \right) = { \frac { 2 ( 2 - p ) } { p } } = { \frac { 2 ( 2 n - b ) } { b } } .
$$

Therefore,

$$
2 c \left( 1 + \frac { 1 - p } { \gamma } \right) \leqslant \frac { 8 \sigma _ { b } \eta _ { \operatorname* { m a x } } M ^ { 2 } n ( 2 n - b ) } { b ^ { 3 } } = \Gamma _ { b } \eta _ { \operatorname* { m a x } } .
$$

The condition in Theorem 3.11 implies

$$
\bar { L } - \frac { 1 } { \eta _ { k } } + 2 c \left( 1 + \frac { 1 - p } { \gamma } \right) \leqslant 0
$$

for every k, because $\eta _ { k } \leqslant \eta _ { \mathrm { m a x } }$ . Hence

$$
\mathbb { E } _ { k } [ F ( x _ { k + 1 } ) + c S _ { k + 1 } ] \leqslant F ( x _ { k } ) + c S _ { k } - \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde { x } _ { k + 1 } ) .
$$

Taking total expectation and summing gives, using $S _ { 0 } = 0 , S _ { K } \geqslant 0$ , and $F ( x _ { K } ) \geqslant F _ { \mathrm { i n f } }$

$$
\sum _ { k = 0 } ^ { K - 1 } \mathbb { E } \left[ \frac { 1 } { \eta _ { k } } D _ { \psi } ( x _ { k } , \widetilde { x } _ { k + 1 } ) \right] \leqslant F ( x _ { 0 } ) - F _ { \mathrm { i n f } } .
$$

Since $\begin{array} { r } { D _ { \psi } ( u , v ) \geqslant \frac 1 2 \| u - v \| ^ { 2 } } \end{array}$ and $\eta _ { k } \leqslant$ η<sub>max</sub>,

$$
\frac { 1 } { 2 \eta _ { \mathrm { m a x } } } \sum _ { k = 0 } ^ { K - 1 } \mathbb { E } \| x _ { k } - \widetilde { x } _ { k + 1 } \| ^ { 2 } \leqslant F ( x _ { 0 } ) - F _ { \mathrm { i n f } } .
$$

For R sampled independently and uniformly from $\{ 0 , \ldots , K - 1 \}$ and $\mathcal { R } _ { R } = ( x _ { R } - \widetilde { x } _ { R + 1 } ) / \eta _ { \mathrm { m a x } }$ this yields

$$
\mathbb { E } \Vert \mathcal { R } _ { R } \Vert ^ { 2 } \leqslant \frac { 2 ( F ( x _ { 0 } ) - F _ { \mathrm { i n f } } ) } { K \eta _ { \mathrm { m a x } } } .
$$

## B Additional experiments

## B.1 Additional experiments for logistic loss

Figures 7–9 extend the initial-step experiment in Figure 3 to batch sizes 1, 16, and 64. Ada-BPSG is denoted by SAGA-BB in this Euclidean setting.

Across these batch sizes, SAGA-BB preserves the broad low-gradient region observed at $b = 8$ , whereas the nonadaptive baselines remain sensitive to the initial scale. This consistency supports the intended role of the adaptive refresh: it reduces dependence on both the starting step and the number of component updates aggregated at each iteration.

## B.2 Additional experiments for Huber loss

We next replace logistic loss by Huber loss in the same $\ell _ { 2 } { \mathrm { - r e g u l a r i z e d } }$ binary-classification problems. The Huber penalty is quadratic for residuals near zero and linear beyond a fixed threshold, combining local smoothness with reduced sensitivity to large residuals. This change tests whether the step-size behavior persists across loss geometries as well as batch sizes.

Figure 10 reproduces the gradient-eficiency pattern observed with logistic loss: SAGA-BB reaches the lowest objective gap in fewer efective passes on each dataset. Figures 11–14 test the complementary stability question across four batch sizes; the consistently broad low-gradient region ties the empirical robustness to the stabilized adaptive mechanism rather than to a single logistic-regression configuration.

![](images/231bf1111ffa2abbd273ad44695278cc873b43162d54893eccf95551e4bacb4a.jpg)  
(a)

![](images/9ee4b9a8cd528b578052abe47455e710a0c5faa5131968bc18c488f47e949f04.jpg)  
(b)

![](images/298c3001cb0884d302d0f688b41ede7b1f91b5a9f2051252e7a7e837507a64b8.jpg)  
(c)

![](images/60cbe050db8a3372809955ee59b93c741b1f065775eb77be42a375b3d5804d41.jpg)  
(d)

Figure 7: Initial-step sensitivity for logistic loss with batch size b = 1. Gradient norms are capped at 1 for readability.  
![](images/8a8f1a7a216660b0dddf47158511e26669866103a1dc5ab8c47731634cd599e5.jpg)  
(a)

![](images/4c28c1b86c58d9c6f1dffa24bd381307a47ee9dbdb2a123cf5c538e5018578d9.jpg)  
(b)

![](images/6ea889f6bbc35f2aab957a034f9c927f0c0fec81a268b34d8a506258d52e541c.jpg)  
(c)

![](images/c38fb3e342432ae16d6b326bad8bcb2960a929df52a2fef27705ab61c57a1ea3.jpg)  
(d)

Figure 8: Initial-step sensitivity for logistic loss with batch size b = 16. Gradient norms are capped at 1 for readability.  
![](images/dbf2e1d475464bc424e0b8eade9141d68309bf15074d25e9243d677b6cacdc93.jpg)  
(a)

![](images/d4f6cbeb654b2026bd3288c36a2d2c4393f2a4d262f2c1d028e8c71f4a894589.jpg)  
(b)

![](images/794cfd61fac8cb2efcf6ab2cd051dd7eea30ed5618afc4da6fb6fc52e0fc446a.jpg)  
(c)

![](images/6fda523cfe2e8c355b3ecf4e0a12e5e90a332796e8d20412085f397d499b0c68.jpg)  
(d)

Figure 9: Initial-step sensitivity for logistic loss with batch size b = 64. Gradient norms are capped at 1 for readability.  
![](images/432e13e911a6f19294c8e021e7b213702abb94a6cbf404d746d4c9ee652d6809.jpg)  
(a)

![](images/0fe499fbed0aea4b1cf0d36455276ab025403e4d3d50a758d7a8338865a2bce8.jpg)  
(b)

![](images/bddd89e8c7dee40c46b34a87b88509b665c766bb3c51be5b7ffa2efc425cad61.jpg)  
(c)

![](images/aa90478440a67119e3dfe98e078f80caac63954f91da500b01e0795dd2ec61f4.jpg)  
(d)

Figure 10: Gradient eficiency on four LibSVM datasets with Huber loss. Lines and shaded regions show the mean and standard deviation over five runs.  
![](images/19960667c79ebea0435bc946d8d37e49a878b773553b7765f08d74e0e187d437.jpg)  
(a)

![](images/7c79202ceea247894b82e18ebd174a19b313fc3e62cdac089915017185387dfe.jpg)  
(b)

![](images/b0c8972143a78fdefdf8213eb6aec97dd78606574837a581e0bd380e6444e968.jpg)  
(c)

![](images/194738937d6a3cc5a5a283d4b5c68bf27a7a94ed4b98ee981d3d17c2ae102f68.jpg)  
(d)  
Figure 11: Initial-step sensitivity for Huber loss with batch size b = 1. Gradient norms are capped at 1 for readability.

![](images/3a939ba3e59c3d47a3684d446cdd1828995cc900d9ac988bd708030db7ee61a1.jpg)  
(a)

![](images/4145f1ecff526170181d2e6175859b5daede0d1474187edc30c538e4300e0056.jpg)  
(b)

![](images/05e927f51cfb791cbe1f66a49f48aea8a8c2a3b91d1f6e8ab7d70f8a41f7fea6.jpg)  
(c)

![](images/cf9781ff678836b18ca00463ded42150409c08d61fdafa7496145b4fc7ed58f6.jpg)  
(d)

Figure 12: Initial-step sensitivity for Huber loss with batch size b = 8. Gradient norms are capped at 1 for readability.  
![](images/2f4d019d0039d120949461642a6b33e309c252817bfe0749b9ac87d256667791.jpg)  
(a)

![](images/d1d224ffe0d9b7174b8dafad4c04bf4a1a79b8aa6bc1c3136c214d6e45b6eef2.jpg)  
(b)

![](images/3b868b4edb80b14b0e89c6be1865e0b6a97d40dd1d6b783aec5d8728dc58d8bf.jpg)  
(c)

![](images/034decd7e77f37f42cc79208c34d74515c2bd9bb7750495d8651a21a662d8b82.jpg)  
(d)

Figure 13: Initial-step sensitivity for Huber loss with batch size b = 16. Gradient norms are capped at 1 for readability.  
![](images/5d5e2270dcb9d98fe8b1cb96ec816230d48aa0ece7c31ce7da0a0494f6bb074d.jpg)  
(a)

![](images/2be7630209300a9cb685672e76d21027beb01c1916ca0ff28cec0462ffaec8fa.jpg)  
(b)

![](images/65ad0d9638d0ab8cb23370cda786add4d34ef8632e6513e355362895719b76b8.jpg)  
(c)

![](images/8f4067cda48815fb343e325b09809e0791e555756da8007ad2215c99b72d127a.jpg)  
(d)  
Figure 14: Initial-step sensitivity for Huber loss with batch size b = 64. Gradient norms are capped at 1 for readability.