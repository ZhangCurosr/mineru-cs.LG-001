# Online Inference for Quantile Temporal Diference Learning in Distributional Reinforcement Learning

Zijie Cheng<sup>∗</sup> Yang Peng<sup>†</sup> Zhihua Zhang<sup>‡</sup>

August 14, 2026

## Abstract

In this paper, we study how to perform statistical inference for quantile temporal diference learning (QTD) in distributional reinforcement learning. Assuming access to a generative model, we first establish functional central limit theorems for both synchronous and asynchronous QTD, which show that the averaged iterates of QTD converge weakly to a rescaled Brownian motion. We next provide online inference methods. Based on random scaling, the inference procedure constructs an asymptotically pivotal statistic for inference by using the information along the whole QTD path. Meanwhile, the proposed statistic can be computed online without storing the entire trajectory of QTD iterates. This substantially reduces the memory requirement and enables eficient statistical inference in distributional reinforcement learning.

## 1 Introduction

Distributional reinforcement learning (DRL) [17, 1] extends the classical reinforcement learning framework by modeling the entire distribution of the return rather than only its expectation. This distributional perspective provides a richer characterization of the uncertainty associated with learning agents and has led to a series of successful applications in practice [2, 4]. Since return distributions are generally infinite-dimensional objects, practical DRL methods rely on finitedimensional approximations. Among these approaches, quantile representations have become one of the most widely used paradigms, originating from Dabney et al. [8], which proposed the quantile temporal diference learning (QTD). By approximating return distributions through a collection of quantiles, quantile-based methods provide a flexible representation while retaining important distributional information, leading to algorithms such as quantile regression deep Q-network (QR-DQN) [8] and implicit quantile network (IQN) [7].

Despite the empirical success of QTD and its variants, the statistical understanding of QTD remains limited. In practical reinforcement learning applications, the transition dynamics and reward distributions are unknown, and the learned quantile representations involve statistical uncertainty due to finite samples. Existing theoretical studies of QTD have mainly focused on convergence and consistency results, while its statistical properties, including asymptotic sampling fluctuations and valid uncertainty quantification, remain largely unexplored. This motivates a fundamental question: can QTD support statistically valid estimation and inference for the quantile representation of return distributions?

Answering these questions for QTD is challenging due to the unique structure of quantile projected Bellman operators. In categorical distributional reinforcement learning, the support locations are fixed and the parametrization is described by a finite-dimensional probability vector. As a result, the corresponding categorical-projected Bellman update acts linearly on the probability vectors, which makes it amenable to tools developed for linear stochastic approximation. In contrast, quantile representations parameterize return distributions through inverse distribution functions. Consequently, QTD leads to a nonlinear and non-smooth stochastic approximation recursion, where the stochastic updates are generated by indicator-type transformations of the quantile parameters. Therefore, the resulting non-smooth dependence across diferent quantile levels prevents a direct application of existing stochastic approximation inference theory and requires a dedicated analysis of the asymptotic behavior of QTD iterates.

In this paper, we develop a statistical inference theory for QTD, a representative quantile-based distributional reinforcement learning algorithm. We establish functional central limit theorems and asymptotic inference procedures for QTD, providing a unified understanding of its statistical behavior.

## 1.1 Our Contributions

We study distributional policy evaluation with quantile parametrization in a tabular γ-discounted Markov decision process. For a given policy π and quantile level $m ,$ let $\eta _ { m }$ denote the unique fixed point of the projected distributional Bellman equation $\pmb { \eta } = \pmb { \Pi } _ { m } \tau ^ { \pi } \pmb { \eta }$ , where $\mathbf { I I } _ { m }$ is the quantile projection operator and $\tau ^ { \pi }$ is the distributional Bellman operator. Our goal is to characterize the asymptotic distribution of quantile temporal-diference learning and to develop valid online inference procedures based on the QTD iterates.

Our main contributions are summarized as follows. Assuming access to a generative model, we establish functional central limit theorems in Theorem 3.1 and 3.2 for the iterates of both synchronous and asynchronous QTD. In particular, after suitable normalization, the partial-sum processes of the iterates converge weakly to rescaled Brownian motions. We also provide explicit characterizations of the corresponding asymptotic covariance matrices. Building on the functional central limit theorems, we develop inference procedures based on random scaling for QTD in Theorem 3.3. We construct an asymptotically pivotal statistic, avoiding the need to explicitly estimate the asymptotic covariance matrices. Moreover, the quantities in the statistic can be updated recursively along the QTD trajectory, so the procedures can be implemented online without storing the entire sequence of iterates.

## 1.2 Related Works

## 1.2.1 Distributional Reinforcement Learning.

Distributional reinforcement learning studies the entire distribution of the return rather than only its expectation [1]. Several finite-dimensional representations have been developed, including categorical [1], quantile [8], and generative-model-based representations [10, 9]. Among them, quantile representations have become a widely used paradigm. Starting from quantile temporal-diference learning [8], subsequent works developed more flexible parametrizations, including implicit quantile networks [7] and fully parameterized quantile functions [27].

The theoretical literature on distributional reinforcement learning has mainly focused on convergence and finite-sample guarantees. For categorical representations, Rowland et al. [22] established asymptotic convergence of categorical temporal-diference learning, while Peng et al. [18] derived non-asymptotic convergence rates and sample complexity guarantees. Furthermore, Peng et al. [19] characterized its finite-sample behavior under linear function approximation. For quantile-based methods, Rowland et al. [23] established asymptotic convergence of quantile temporal-diference learning. However, their analysis focuses on consistency and does not characterize the asymptotic distribution of the iterates or provide statistical inference procedures. Separately, Rowland et al. [24] decomposed the estimation error of quantile temporal-diference learning into fixed-point bias and finite-sample variance, showing that it can outperform classical temporal diference learning for

expected return estimation.

## 1.2.2 Statistical Analysis of Reinforcement Learning.

Statistical inference for reinforcement learning has received increasing attention, with most existing work focusing on value functions. Representative results include high-confidence bounds for of-policy evaluation [26, 13], bootstrap-based inference [11], asymptotic inference for robust value functions [28], confidence intervals for value functions under policy evaluation and policy learning [25, 30], and online inference procedures [16, 15].

Comparatively fewer works study inference for return distributions and other distributional quantities. Chandak et al. [5] and Huang et al. [12] developed procedures for estimating return distribution functions and constructing confidence bands, while Qi et al. [21] studied distributional of-policy evaluation under ofline data. Under a generative model, Zhang et al. [29] developed estimation and inference theory for return distributions, and Cheng et al. [6] studied model-based estimation and inference for quantile representations. In contrast, we focus on performing statistical inference for quantile temporal diference learning, a widely used model-free algorithm in quantile distributional reinforcement learning.

The remainder of this paper is organized as follows. In Section 2, we introduce some basic concepts of distributional reinforcement learning. In Section 3, we present our statistical analysis of quantile temporal diference learning. In Section 4, we provide an outlined proof of results in Section 3. We conclude our work in Section 5. Details of the proof are given in the appendices.

## 2 Preliminaries

In this section, we introduce the necessary background for our work. We review the Markov decision process in Section 2.1, followed by metrics on the space of measures in Section 2.2. We introduce distributional Bellman operator and quantile projection operator in Section 2.3. Finally, in Section 2.5, we present the assumptions under which our main theoretical results are established.

## 2.1 Problem Setup

We consider a discounted Markov decision process (MDP) specified by the tuple $\mathcal { M } = \langle \boldsymbol { S } , \mathcal { A } , \mathcal { P } _ { R } , \boldsymbol { P } , \gamma \rangle$ where S and A are finite state space and finite action space respectively, $\mathcal { P } _ { R } \colon \mathcal { S } \times \mathcal { A } \to \Delta ( [ 0 , 1 ] )$ is the distribution of rewards, $P \colon S \times A \to \Delta ( S )$ is the transition probability, and $\gamma \in ( 0 , 1 )$ is the

discount factor. Here $\Delta ( \cdot )$ denotes the set of probability distributions over some set.

For a fixed policy $\pi \colon S \to \Delta ( { \mathcal { A } } )$ and an initial state $S _ { 0 } ~ = ~ s ~ \in ~ { \mathcal S } .$ , a random trajectory $\{ ( S _ { t } , A _ { t } , R _ { t } ) \} _ { t = 0 } ^ { \infty }$ can be sampled from the MDP using the following procedure:

$$
A _ { t } \mid S _ { t } \sim \pi ( \cdot \mid S _ { t } ) ,
$$

$$
R _ { t } \mid ( S _ { t } , A _ { t } ) \sim { \mathcal { P } } _ { R } ( \cdot \mid S _ { t } , A _ { t } ) ,
$$

$$
S t { + } 1 \mid ( S _ { t } , A _ { t } ) \sim P ( \cdot \mid S _ { t } , A _ { t } ) .
$$

The return of such a trajectory starting from state s is defined as the random variable

$$
G ^ { \pi } ( s ) : = \sum _ { t = 0 } ^ { \infty } \gamma ^ { t } R _ { t } ,
$$

which is bounded almost surely by $[ 0 , ( 1 - \gamma ) ^ { - 1 } ]$ . The value function $V ^ { \pi } ( s )$ is defined by the expected return $\mathbb { E } [ G ^ { \pi } ( s ) ]$ . We further denote by $\eta ^ { \pi } ( s ) \in \Delta ( [ 0 , ( 1 - \gamma ) ^ { - 1 } ] )$ the distribution of $G ^ { \pi } ( s )$ , and denote $\eta ^ { \pi } = ( \eta ^ { \pi } ( s ) ) _ { s \in { \cal S } }$ for the collection of return distributions across all states.

## 2.2 Metrics on the Space of Measures

Denote the space of all probability distributions on $\mathbb { R }$ as $\mathcal { P }$ . For $\mu \in \mathcal { P }$ , the cumulative distribution function is defined as $F _ { \mu } ( x ) \ = \ \mu ( - \infty , x ]$ , and the quantile function is defined as $F _ { \mu } ^ { - 1 } ( \tau ) = \operatorname* { i n f } \{ x \colon F _ { \mu } ( x ) \geqslant \tau \}$ . For $1 \leqslant p < \infty$ and $\mu , \nu \in { \mathcal { P } }$ , the $p \textmd { - }$ Wasserstein metric between $\mu$ and $\nu$ is defined as

$$
W _ { p } ( \mu , \nu ) = \left( \int _ { 0 } ^ { 1 } \left| F _ { \mu } ^ { - 1 } ( t ) - F _ { \nu } ^ { - 1 } ( t ) \right| ^ { p } \mathrm { d } t \right) ^ { 1 / p } ,
$$

and the 8-Wasserstein metric is defined as

$$
W _ { \infty } ( \mu , \nu ) = \operatorname * { s u p } _ { t \in [ 0 , 1 ] } \left| F _ { \mu } ^ { - 1 } ( t ) - F _ { \nu } ^ { - 1 } ( t ) \right| .
$$

Moreover, for any extended metric d: ${ \mathcal { P } } \times { \mathcal { P } }  [ 0 , \infty ]$ , we can define its supremum extension <sup>¯</sup>d: $\mathcal { P } ^ { S } \times \mathcal { P } ^ { S }  [ 0 , \infty ]$ as

$$
\bar { d } ( \eta , \eta ^ { \prime } ) = \operatorname* { s u p } _ { s \in { \mathcal { S } } } d ( \eta ( s ) , \eta ^ { \prime } ( s ) ) ,
$$

which is an extended metric on $\mathcal { P } ^ { S }$

## 2.3 Distributional Bellman Operator and Quantile Projection Operator

A fundamental property of the value function is that it satisfies the Bellman equation. Letting $V ^ { \pi } : = ( V ^ { \pi } ( s ) ) _ { s \in { \cal S } }$ , we have for any $s \in \mathcal S$

$$
\begin{array} { r l } & { V ^ { \pi } ( s ) = \left[ T ^ { \pi } V ^ { \pi } \right] ( s ) } \\ & { \qquad : = { \mathbb E } _ { A \sim \pi ( \cdot \vert s ) , R \sim \mathcal { P } ( \cdot \vert s , A ) } [ R ] + { \mathbb E } _ { A \sim \pi ( \cdot \vert s ) , S ^ { \prime } \sim P ( \cdot \vert s , A ) } [ V ^ { \pi } ( S ^ { \prime } ) ] } \\ & { \qquad = \displaystyle \sum _ { a \in \mathcal { A } } \pi ( a \mid s ) \int _ { 0 } ^ { 1 } r \mathcal { P } _ { R } ( \mathrm { d } r \mid s , a ) + \sum _ { a \in \mathcal { A } , s ^ { \prime } \in S } \pi ( a \mid s ) P ( s ^ { \prime } \mid s , a ) V ^ { \pi } ( s ^ { \prime } ) . } \end{array}\tag{1}
$$

The operator $T ^ { \pi } \colon { \mathbb { R } ^ { S } }  { \mathbb { R } ^ { S } }$ is referred to as the Bellman operator, and Equation (1) characterizes $V ^ { \pi }$ as its unique fixed point.

An analogous relationship holds for the return distributions $\eta ^ { \pi }$ , known as the distributional Bellman equation. That is, for each $s \in \mathcal S$

$$
\begin{array} { r l } & { \eta ^ { \pi } ( s ) = \left[ \mathcal { T } ^ { \pi } \eta ^ { \pi } \right] ( s ) } \\ & { \qquad : = { \mathbb E } _ { A \sim \pi ( \cdot | s ) , R \sim \mathcal { P } _ { R } ( \cdot | s , A ) , S ^ { \prime } \sim P ( \cdot | s , A ) } \left[ ( b _ { R , \gamma } ) _ { \# } \eta ^ { \pi } ( S ^ { \prime } ) \right] } \\ & { \qquad = \displaystyle \sum _ { a \in \mathcal { A } , s ^ { \prime } \in S } \pi ( a \mid s ) P ( s ^ { \prime } \mid s , a ) \displaystyle \int _ { 0 } ^ { 1 } ( b _ { r , \gamma } ) _ { \# } \eta ^ { \pi } ( s ^ { \prime } ) \mathcal { P } _ { R } ( \mathrm { d } r \mid s , a ) . } \end{array}
$$

Here $b _ { r , \gamma } \colon \mathbb { R } \to  { \mathbb { R } }$ denotes the afine map $b _ { r , \gamma } ( x ) = r + \gamma x$ , and $g _ { \# } \mu$ is the pushforward of a measure $\mu$ under g, defined by $g _ { \# } \mu ( B ) = \mu ( g ^ { - 1 } ( B ) )$ for all Borel sets B. The integral $\int _ { 0 } ^ { 1 } { ( b _ { r , \gamma } ) _ { \# } \eta ^ { \pi } ( s ^ { \prime } ) \mathcal { P } _ { R } ( \mathrm { d } r } \mid s , a )$ is defined in the sense that for any Borel set $B { \mathrm { , } }$

$$
\left[ \int _ { 0 } ^ { 1 } \left( b _ { r , \gamma } \right) _ { \# } \eta ^ { \pi } ( s ^ { \prime } ) \mathcal { P } _ { R } ( \mathrm { d } r \mid s , a ) \right] ( B ) = \int _ { 0 } ^ { 1 } \left[ ( b _ { r , \gamma } ) _ { \# } \eta ^ { \pi } ( s ^ { \prime } ) \right] ( B ) \mathcal { P } _ { R } ( \mathrm { d } r \mid s , a ) .
$$

Recall that $\mathcal { P }$ is the space of all probability measures on $\mathbb { R } ,$ and $\mathcal T ^ { \pi } \colon \mathcal P \to \mathcal P$ is referred to as the distributional Bellman operator, of which the fixed point is the return distribution $\eta ^ { \pi }$

Because the exact distribution $\eta ^ { \pi }$ is infinite-dimensional and cannot be computed exactly, we approximate it using a quantile-parameterized distribution. The space of all quantile-parametrized probability distributions is defined as

$$
\mathcal { P } _ { m } : = \left\{ \nu _ { \pm } = \frac { 1 } { m } \sum _ { i = 1 } ^ { m } \delta _ { x _ { i } } \mid x = ( x _ { 1 } , \ldots , x _ { m } ) ^ { \top } \in \mathbb { R } ^ { m } , x _ { 1 } \leqslant \ldots \leqslant x _ { m } \right\} ,
$$

which is a mixture of Dirac measures and $m \in \mathbb { N }$ . We define the quantile projection operator

$\Pi _ { m } \colon { \mathcal { P } } \to { \mathcal { P } } _ { m }$ as

$$
\Pi _ { m } \nu = \frac { 1 } { m } \sum _ { i = 1 } ^ { m } \delta _ { F _ { \nu } ^ { - 1 } ( \tau _ { i } ) } ,
$$

where $\begin{array} { r } { \tau _ { i } = \frac { 2 i - 1 } { 2 m } } \end{array}$ . We lift $\Pi _ { m }$ to the product space $\mathcal { P } ^ { S }$ by defining $( \boldsymbol { \Pi } _ { m } \pmb { \eta } ) ( s ) : = \Pi _ { m } \eta ( s )$ for any $\pmb { \eta } = ( \eta ( s ) ) _ { s \in \mathcal { S } } \in \mathcal { P } ^ { S }$ . The properties of $\tau ^ { \pi }$ and $\mathbf { I I } _ { m }$ are summarized in the following proposition.

Proposition 2.1. [3, Proposition 4.15, Lemma 5.25] The following statements hold:

$\mathcal { T } ^ { \pi }$ is γ-contractive under the $\bar { W } _ { p }$ metric for every $p \in [ 1 , \infty ]$ , namely for every $\eta , \eta ^ { \prime } \in \mathcal { P } ^ { S }$

$$
\bar { W } _ { p } ( T ^ { \pi } \eta , T ^ { \pi } \eta ^ { \prime } ) \leqslant \gamma \bar { W } _ { p } ( \eta , \eta ^ { \prime } ) ;
$$

$\mathbf { I I } _ { m }$ is non-expansive under the $\bar { W } _ { \infty }$ metric, namely $\bar { W } _ { \infty } ( \Pi _ { m } \eta , \Pi _ { m } \eta ^ { \prime } ) \leqslant \bar { W } _ { \infty } ( \eta , \eta ^ { \prime } )$ for every $\eta , \eta ^ { \prime } \in \mathcal { P } ^ { S }$

It immediately follows from Proposition 2.1 that the quantile projected Bellman operator $\Pi _ { m } \mathcal { T } ^ { \pi }$ is a γ-contraction in the Polish space $( \mathcal { P } ^ { S } , \bar { W } _ { \infty } )$ . Hence, the quantile projected Bellman equation $\pmb { \eta } = \pmb { \Pi } _ { m } \tau ^ { \pi } \pmb { \eta }$ admits a unique solution $\eta _ { m }$ . Denote $[ m ] = \{ 1 , \dots , m \}$ , and we define the quantile parameter $\pmb { \theta } _ { m } \in \mathbb { R } ^ { S \times [ m ] }$ by

$$
\eta _ { m } ( s ) = \frac { 1 } { m } \sum _ { i = 1 } ^ { m } \delta _ { \theta _ { m } ( s , i ) } ,
$$

with $\theta _ { m } ( s , 1 ) \leqslant \cdots \leqslant \theta _ { m } ( s , m )$ for every $s \in \mathcal S$

Finally, we need a generalized version of quantile projection operator. For $\pmb { \lambda } \in [ 0 , 1 ] ^ { S \times [ m ] }$ , define $\Pi _ { m } ^ { \lambda }$ by

$$
( \Pi _ { m } ^ { \lambda } \eta ) ( s ) = { \frac { 1 } { m } } \sum _ { i = 1 } ^ { m } \delta _ { ( 1 - \lambda ( s , i ) ) F _ { \eta ( s ) } ^ { - 1 } ( \tau _ { i } ) + \lambda ( s , i ) \bar { F } _ { \eta ( s ) } ^ { - 1 } ( \tau _ { i } ) } ,
$$

where $\bar { F } _ { \eta ( s ) } ^ { - 1 } ( \tau ) = \operatorname* { i n f } \{ x : F _ { \eta ( s ) } ( x ) > \tau \}$ . It is proved in Rowland et al. [23] that every $\Pi _ { m } ^ { \lambda }$ is non-expensive under the $\bar { W } _ { \infty }$ metric. Therefore, every $\Pi _ { m } ^ { \lambda } T ^ { \pi }$ has a unique fixed point.

## 2.4 Quantile Temporal Diference Learning

We introduce quantile temporal diference learning (QTD) in this section. We introduce the synchronous case first. In this setting, at every interation t, for every $s \in \mathcal S$ , we sample

$$
a ^ { ( t , s ) } \sim \pi ( \cdot \mid s ) , \left( r ^ { ( t , s ) } s ^ { \prime ( t , s ) } \right) \sim Q _ { s , a ^ { ( t , s ) } }
$$

from a generative model and update

$$
\theta ^ { ( t ) } ( s , i ) = \theta ^ { ( t - 1 ) } ( s , i ) + \alpha _ { t - 1 } \left[ \tau _ { i } - \frac { 1 } { m } \sum _ { j = 1 } ^ { m } \mathbb { 1 } \left\{ r ^ { ( t , s ) } + \gamma \theta ^ { ( t - 1 ) } \left( s ^ { \prime ( t , s ) } , j \right) < \theta ^ { ( t - 1 ) } ( s , i ) \right\} \right] ,\tag{2}
$$

where we define

$$
Q _ { s , a } : = { \mathcal { P } } _ { R } ( \cdot \mid s , a ) \otimes P ( \cdot \mid s , a ) .
$$

In an asynchronous setting, at every iteration t, we sample

$$
s ^ { ( t ) } \sim \mu ( \cdot ) , a ^ { ( t ) } \sim \pi ( \cdot \mid s ^ { ( t ) } ) , \left( r ^ { ( t ) } s ^ { \prime ( t ) } \right) \sim Q _ { s ^ { ( t ) } , a ^ { ( t ) } }
$$

from a generative model and update

$$
\theta ^ { ( t ) } ( s , i ) = \theta ^ { ( t - 1 ) } ( s , i ) + \alpha _ { t - 1 } \mathbb { 1 } \left\{ s = s ^ { ( t ) } \right\} \left[ \tau _ { i } - \frac { 1 } { m } \sum _ { j = 1 } ^ { m } \mathbb { 1 } \left\{ r ^ { ( t ) } + \gamma \theta ^ { ( t - 1 ) } \left( s ^ { \prime ( t ) } , j \right) < \theta ^ { ( t - 1 ) } ( s , i ) \right\} \right] .\tag{3}
$$

## 2.5 Main Assumptions

For every $( s , a ) \in S \times A$ , we make the following assumption about $\mathcal { P } _ { R } ( \cdot \mid s , a )$

Assumption 1. For any $s \in \mathcal S , a \in \mathcal A , \mathcal P _ { R } ( \cdot \mid s , a )$ is supported on $[ 0 , 1 ]$ and has a Lebesgue density $p _ { s , a }$ . Moreover, $p _ { s , a }$ is Lipschitz continuous on $[ 0 , 1 ]$ and there exists a positive constant $C _ { 0 }$ such that $0 < p _ { s , a } ( x ) \leqslant C _ { 0 }$ for any $x \in ( 0 , 1 )$

Moreover, we make an assumption on the locations of $\pmb { \theta } _ { m }$

Assumption 2. For every $s , s ^ { \prime } \in \mathcal { S }$ satisfying

$$
P ^ { \pi } ( s ^ { \prime } \mid s ) : = \sum _ { a \in \mathcal { A } } \pi ( a \mid s ) P ( s ^ { \prime } \mid s ) > 0 ,
$$

we have

$$
\theta _ { m } ( s , i ) - \gamma \theta _ { m } ( s ^ { \prime } , j ) \notin \{ 0 , 1 \}
$$

for every $i , j \in [ m ]$

This is a technical condition that excludes boundary-degenerate configurations in which Bellmanshifted quantile locations coincide with the endpoints of the reward support. In particular, it

guarantees that the projected Bellman equation is continuously diferentiable around the true parameter, thereby ensuring the regularity conditions required by the Z-estimation theory used in subsequent analysis. We suppose that the two assumptions above hold throughout the paper.

## 3 Main Results

## 3.1 Functional Central Limit Theorem for QTD

In this section, we present functional central limit theorems (FCLT) for QTD. Let $B ( \cdot )$ denote an $s \times [ m ]$ -dimensional standard Brownian motion on r0, 1s. We first need to introduce some matrices in preparation.

For a distribution ν on $\mathcal { S } , D _ { \nu }$ is defined as $\mathrm { d i a g } _ { s } \{ \nu ( s ) I _ { m } \}$ . G is defined as

$$
\begin{array} { l } { { \displaystyle { \cal G } _ { ( s , i ) , ( s ^ { \prime } , j ) } } } \\ { { \displaystyle = - \frac { \gamma } { m } \sum _ { a \in \mathcal { A } } \pi ( a \mid s ) P ( s ^ { \prime } \mid s , a ) p _ { s , a } ( \theta _ { m } ( s , i ) - \gamma \theta _ { m } ( s ^ { \prime } , j ) ) } } \\ { { \displaystyle ~ + \frac { 1 \{ ( s , i ) = ( s ^ { \prime } , j ) \} } { m } \sum _ { a \in \mathcal { A } , \tilde { s } \in \mathcal { S } } \sum _ { k = 1 } ^ { m } \pi ( a \mid s ) P ( \tilde { s } \mid s , a ) p _ { s , a } \left( \theta _ { m } ( s , i ) - \gamma \theta _ { m } ( \tilde { s } , k ) \right) . } } \end{array}
$$

For $s \in \mathcal S$ and $i \in [ m ]$ , define

$$
\xi _ { s , i } ( a , r , s ^ { \prime } ) : = \frac { 1 } { m } \sum _ { j = 1 } ^ { m } 1 \left\{ r + \gamma \theta _ { m } ( s ^ { \prime } , j ) < \theta _ { m } ( s , i ) \right\} - \tau _ { i } .
$$

The covariance matrices $\mathbf { \Gamma } _ { \mathbf { S y n } }$ and $\mathbf { \Gamma } _ { \mathbf { a s y n } }$ are defined by

$$
\begin{array} { r } { ( \Gamma _ { \mathrm { s y n } } ) _ { ( s , i ) , ( \tilde { s } , k ) } = \mathbb { 1 } \left\{ s = \tilde { s } \right\} \mathsf { C o v } _ { A \sim \pi ( \cdot | s ) , ( R , S ^ { \prime } ) \sim Q _ { s , A } } \left[ \xi _ { s , i } ( A , R , S ^ { \prime } ) , \xi _ { \tilde { s } , k } ( A , R , S ^ { \prime } ) \right] ; } \end{array}\tag{4}
$$

$$
\begin{array} { r } { ( \Gamma _ { \mathrm { a s y n } } ) _ { ( s , i ) , ( \tilde { s } , k ) } = \mathsf { C o v } _ { S \sim \mu , A \sim \pi ( \cdot | S ) , ( R , S ^ { \prime } ) \sim Q _ { S , A } } \left[ \mathbb { 1 } \left\{ S = s \right\} \xi _ { s , i } ( A , R , S ^ { \prime } ) , \mathbb { 1 } \left\{ S = \tilde { s } \right\} \xi _ { \tilde { s } , k } ( A , R , S ^ { \prime } ) \right] } \end{array}\tag{5}
$$

In particular, we can verify that $\Gamma _ { \mathrm { a s y n } } = D _ { \mu } \Gamma _ { \mathrm { s y n } }$ . Now we can formulate the FCLT.

Theorem 3.1. For update rules (2), as $T \to \infty$ ，

$$
\frac { 1 } { \sqrt { T } } \sum _ { t = 0 } ^ { \lfloor T u \rfloor } \Big ( \pmb { \theta } ^ { ( t ) } - \pmb { \theta } _ { m } \Big ) \Rightarrow \pmb { G } ^ { - 1 } \mathbf { \Gamma } _ { \mathrm { s y n } } ^ { \frac { 1 } { 2 } } \pmb { B } ( \cdot ) .
$$

Theorem 3.2. For update rules (3), as $T \to \infty$

$$
\frac { 1 } { \sqrt { T } } \sum _ { t = 0 } ^ { \lfloor T u \rfloor } \Big ( \pmb { \theta } ^ { ( t ) } - \pmb { \theta } _ { m } \Big ) \Rightarrow ( D _ { \mu } \pmb { G } ) ^ { - 1 } \mathbf { \Gamma } _ { \mathrm { a s y n } } ^ { \frac { 1 } { 2 } } \pmb { B } ( \cdot ) .
$$

## 3.2 Online Inference Procedure for QTD

In this section, we provide an online statistical inference procedure for QTD based on the FCLTs established above. The proposed procedure is based on random scaling and does not require estimating either the Jacobian matrix G or the covariance matrices $\mathbf { \Gamma } _ { \mathbf { s y n } }$ and $\mathbf { \Gamma } _ { \mathbf { a s y n } }$

Let

$$
\bar { \pmb { \theta } } _ { T } : = \frac { 1 } { T } \sum _ { t = 1 } ^ { T } { \pmb { \theta } } ^ { ( t ) }
$$

be the Polyak–Ruppert averaged QTD iterate. Define the random scaling matrix

$$
\widehat { V } _ { T } : = \frac { 1 } { T ^ { 3 } } \sum _ { t = 1 } ^ { T } \left( \sum _ { k = 1 } ^ { t } \pmb { \theta } ^ { ( k ) } - t \bar { \pmb { \theta } } _ { T } \right) \left( \sum _ { k = 1 } ^ { t } \pmb { \theta } ^ { ( k ) } - t \bar { \pmb { \theta } } _ { T } \right) ^ { \top } .\tag{6}
$$

Equivalently, denoting

$$
\bar { \pmb { \theta } } _ { t } : = \frac { 1 } { t } \sum _ { k = 1 } ^ { t } \pmb { \theta } ^ { ( k ) } ,
$$

we have

$$
{ \widehat { V } } _ { T } = { \frac { 1 } { T ^ { 3 } } } \sum _ { t = 1 } ^ { T } t ^ { 2 } \left( { \bar { \theta } } _ { t } - { \bar { \theta } } _ { T } \right) \left( { \bar { \theta } } _ { t } - { \bar { \theta } } _ { T } \right) ^ { \top } .\tag{7}
$$

Let $B ( \cdot )$ denote a one-dimensional standard Brownian motion on r0, 1s and define the random variable

$$
\mathcal { V } : = \frac { B ( 1 ) } { \left[ \int _ { 0 } ^ { 1 } \left( B ( u ) - u B ( 1 ) \right) ^ { 2 } \mathrm { d } u \right] ^ { 1 / 2 } } ,\tag{8}
$$

which is pivotal.

The following theorem provides a random-scaling inference procedure for linear functionals of the QTD fixed point. In particular, the limiting distribution is the same for synchronous and asynchronous QTD and is independent of the corresponding asymptotic covariance matrix. It is proved in Appendix A.

Theorem 3.3. Let $\pmb { c } \in \mathbb { R } ^ { S \times [ m ] }$ and suppose that

$$
c ^ { \top } G ^ { - 1 } \Gamma _ { \mathrm { s y n } } G ^ { - ^ { \top } } c > 0 , \ c ^ { \top } ( D _ { \mu } G ) ^ { - 1 } \Gamma _ { \mathrm { a s y n } } ( D _ { \mu } G ) ^ { - ^ { \top } } c > 0 .
$$

Then, as $T \to \infty$

$$
\frac { { { c } ^ { \top } } \left( \bar { \theta } _ { T } - \theta _ { m } \right) } { \sqrt { { { c } ^ { \top } } \hat { V } _ { T } { { c } } } } \Rightarrow \mathcal { V } .
$$

Let $q _ { \alpha }$ denote the upper α-quantile of the distribution of V defined in (8). Then an asymptotic confidence interval follows immediately.

Corollary 3.1. Under the conditions of Theorem 3.3, denote

$$
\mathbf { C I } ( \alpha ) = \left[ c ^ { \top } \bar { \theta } _ { T } - q _ { \alpha / 2 } \sqrt { c ^ { \top } \hat { V } _ { T } c } , c ^ { \top } \bar { \theta } _ { T } + q _ { \alpha / 2 } \sqrt { c ^ { \top } \hat { V } _ { T } c } \right] ,
$$

where $q _ { \alpha / 2 }$ is the upper α{2-quantile of V. Then,

$$
\operatorname* { l i m } _ { T \to \infty } \mathbb { P } \left[ c ^ { \top } \pmb \theta _ { m } \in \mathbf { C I } ( \alpha ) \right] = 1 - \alpha .
$$

The random scaling matrix can be updated online without storing the entire trajectory. Define

$$
A _ { t } : = \sum _ { k = 1 } ^ { t } k ^ { 2 } \bar { \pmb { \theta } } _ { k } \bar { \pmb { \theta } } _ { k } ^ { \top } ,
$$

$$
b _ { t } : = \sum _ { k = 1 } ^ { t } k ^ { 2 } { \bar { \theta } } _ { k } ,
$$

$$
q _ { t } : = \sum _ { k = 1 } ^ { t } k ^ { 2 } .
$$

These quantities admit the recursive updates

$$
\bar { \pmb { \theta } } _ { t } = \frac { t - 1 } { t } \bar { \pmb { \theta } } _ { t - 1 } + \frac { 1 } { t } \pmb { \theta } ^ { ( t ) } ,
$$

$$
\pmb { A } _ { t } = \pmb { A } _ { t - 1 } + t ^ { 2 } \pmb { \bar { \theta } } _ { t } \pmb { \bar { \theta } } _ { t } ^ { \top } ,
$$

$$
{ \pmb b } _ { t } = { \pmb b } _ { t - 1 } + t ^ { 2 } { \bar { \pmb \theta } } _ { t } ,
$$

$$
q _ { t } = q _ { t - 1 } + t ^ { 2 } .
$$

Consequently,

$$
\widehat { V } _ { t } = \frac { 1 } { t ^ { 3 } } \left( A _ { t } - \bar { \theta } _ { t } \boldsymbol { b } _ { t } ^ { \top } - \boldsymbol { b } _ { t } \bar { \theta } _ { t } ^ { \top } + q _ { t } \bar { \theta } _ { t } \bar { \theta } _ { t } ^ { \top } \right) .
$$

Thus, both the averaged QTD iterate and its random scaling matrix can be updated sequentially as new QTD iterates become available.

## 4 Proof Outlines

In this section, we present proofs of Theorem 3.1 and 3.2.

## 4.1 Proof of Theorem 3.1 and 3.2

To establish the FCLT for QTD, we first present the following general functional central limit theorem, which is proved in Appendix B.

Theorem 4.1. Suppose that the $\mathbb { R } ^ { d }$ sequence $( Z _ { t } ) _ { t \geqslant 0 }$ is defined by

$$
Z _ { t } = Z _ { t - 1 } - \alpha _ { t - 1 } [ { \pmb h } ( { \pmb Z } _ { t - 1 } ) + { \pmb \epsilon } _ { t - 1 } ] ,
$$

where h is a Borel function with a unique zero $z ^ { * }$ . Suppose there exists a matrix M such that there exists $\delta > 0$ satisfying

$$
\| h ( z ) - M ( z - z ^ { * } ) \| \leqslant L \left\| z - z ^ { * } \right\| ^ { 2 }\tag{9}
$$

for $\| z - z ^ { * } \| \leqslant \delta$ , and M is a uniformly repulsive matrix (all its eigenvalues have positive real parts). Define $( \mathcal { F } _ { t } : = \sigma ( Z _ { 0 } , . . . , Z _ { t + 1 } ) ) _ { t \geqslant - 1 }$ and suppose that

(i) $\epsilon _ { t }$ is $\mathcal { F } _ { t }$ -measurable;

(ii) $\mathbb { E } ( \epsilon _ { t } \mid \mathcal { F } _ { t - 1 } ) = 0 ;$

(iii) there exists $L _ { 1 } > 0$ such that $\| \epsilon _ { t } \| \leqslant L _ { 1 }$ almost surely;

(iv) $\mathbb { E } ( \epsilon _ { t } \epsilon _ { t } ^ { \top } \mid \mathcal { F } _ { t - 1 } )  \boldsymbol { \Gamma }$ almost surely.

Set $\alpha _ { t } = c ( t + 1 ) ^ { - a }$ with $1 / 2 < a < 1$ and suppose that $Z _ { n } \to z ^ { * }$ almost surely. Then

$$
\frac { 1 } { \sqrt { T } } \sum _ { t = 0 } ^ { \lfloor T u \rfloor } ( Z _ { t } - z ^ { * } ) \Rightarrow M ^ { - 1 } \Gamma ^ { \frac { 1 } { 2 } } B ( \cdot ) ,
$$

where $B ( \cdot )$ is a d-dimensional standard Brownian motion.

To apply Theorem 4.1 in QTD settings, we need the following lemmas, the proofs of which are deferred to Appendix B.

Lemma 4.1. For every $\pmb { \lambda } \in [ 0 , 1 ] ^ { S \times [ m ] } , \pmb { \eta } _ { m } = \pmb { \Pi } _ { m } ^ { \lambda } T ^ { \pi } \pmb { \eta } _ { m }$

Lemma 4.2. For update rules (2) and (3), as $T \to \infty , \pmb { \theta } ^ { ( T ) } \to \pmb { \theta } _ { m }$ almost surely.

Lemma 4.3. Both G and $\pmb { { \cal D } } _ { \mu } \pmb { G }$ are uniformly repulsive.

Proof of Theorem 3.1. Define

$$
\begin{array} { c l } { { \displaystyle ( h ( \theta ) ) _ { s , i } = \sum _ { j = 1 } ^ { m } \sum _ { a \in A , s ^ { \prime } \in \mathcal { S } } \frac { \pi ( a \mid s ) P ( s ^ { \prime } \mid s , a ) } { m } F _ { s , a } \big ( \theta ( s , i ) - \gamma \theta ( s ^ { \prime } , j ) \big ) - \tau _ { i } , } } \\ { { \displaystyle \big ( H ^ { ( t ) } ( \theta ) \big ) _ { s , i } = \frac { 1 } { m } \sum _ { j = 1 } ^ { m } \mathbb { 1 } \left\{ r ^ { ( t , s ) } + \gamma \theta \left( s ^ { \prime ( t , s ) } , j \right) < \theta ( s , i ) \right\} - \tau _ { i } . } } \end{array}
$$

By construction, $\mathbb { E } [ H ^ { ( t ) } ( \pmb { \theta } ) ] = h ( \pmb { \theta } )$ . Therefore, the synchronous QTD update can be written as

$$
\begin{array} { r l } & { \pmb { \theta } ^ { ( t ) } = \pmb { \theta } ^ { ( t - 1 ) } - \alpha _ { t - 1 } \left[ h ( \pmb { \theta } ^ { ( t - 1 ) } ) + \pmb { H } ^ { ( t ) } ( \pmb { \theta } ^ { ( t - 1 ) } ) - h ( \pmb { \theta } ^ { ( t - 1 ) } ) \right] } \\ & { \qquad : = \pmb { \theta } ^ { ( t - 1 ) } - \alpha _ { t - 1 } \left[ h ( \pmb { \theta } ^ { ( t - 1 ) } ) + \epsilon _ { t - 1 } \right] . } \end{array}
$$

Since

$$
( h ( \pmb \theta ) ) _ { s , i } = F _ { ( T ^ { \pi } \eta _ { \pmb \theta } ) ( s ) } ( \theta ( s , i ) ) - \tau _ { i } ,
$$

where

$$
\eta _ { \pmb \theta } ( s ) = \frac { 1 } { m } \sum _ { i = 1 } ^ { m } \delta _ { \pmb \theta ( s , i ) } ,
$$

if $\pmb { \theta } ^ { * }$ is a zero of $h ,$ , there exists λ such that $\eta _ { \theta ^ { \ast } }$ is the fixed point of $\Pi _ { m } ^ { \lambda }$ . Therefore, h has a unique zero $\pmb { \theta } _ { m }$ by Lemma 4.1. Moreover, direct diferentiation gives $\nabla h ( \pmb \theta _ { m } ) = G$

By the Lemma 4.3, G is uniformly repulsive. Furthermore, Assumptions 1 and 2 ensure that $\nabla h$ is Lipschitz continuous in a neighborhood of $\pmb { \theta } _ { m }$ . Since $h ( \theta _ { m } ) = 0$ , it follows that

$$
\| h ( \theta ) - G ( \theta - \theta _ { m } ) \| \leqslant L \| \theta - \theta _ { m } \| ^ { 2 }
$$

for all θ in a suficiently small neighborhood of $\pmb { \theta } _ { m }$ , and hence Equation (9) holds.

It remains to verify the noise conditions in Theorem 4.1. Recall that

$$
\epsilon _ { t - 1 } = H ^ { ( t ) } ( \pmb \theta ^ { ( t - 1 ) } ) - h ( \pmb \theta ^ { ( t - 1 ) } ) .
$$

With

$$
\mathcal { F } _ { t } = \sigma \left( \pmb { \theta } ^ { ( 0 ) } , \dots , \pmb { \theta } ^ { ( t + 1 ) } \right) ,
$$

$\epsilon _ { t }$ is $\mathcal { F } _ { t } .$ -measurable. Moreover, since the samples used at iteration t are independent of $\mathcal { F } _ { t - 2 }$ , we have $\mathbb { E } [ \epsilon _ { t } \mid \mathcal { F } _ { t - 1 } ] = 0$ . Since each coordinate of ${ \pmb H } ^ { ( t ) } ( { \pmb \theta } )$ is uniformly bounded, the sequence $( \epsilon _ { t } ) _ { t \geq 0 }$ is uniformly bounded almost surely. Finally, by the almost sure convergence $\pmb { \theta } ^ { ( t ) }  \pmb { \theta } _ { m }$ and the continuity of the corresponding conditional covariance matrix, $\mathbb { E } [ \epsilon _ { t } \epsilon _ { t } ^ { \top } \mid \mathcal { F } _ { t - 1 } ]  \Gamma _ { \mathrm { s y n } }$ almost surely. Thus all the conditions of Theorem 4.1 are satisfied with

$$
M = G , \Gamma = \Gamma _ { \mathrm { s y n } } .
$$

Applying Theorem 4.1 completes the proof.

Proof of Theorem 3.2. Recall the function h defined in the proof of Theorem 3.1. Define

$$
\left( { H } ^ { ( t ) } ( \pmb { \theta } ) \right) _ { s , i } = \mathbb { 1 } \left\{ s = s ^ { ( t ) } \right\} \left[ \frac { 1 } { m } \sum _ { j = 1 } ^ { m } \mathbb { 1 } \left\{ r ^ { ( t ) } + \gamma \theta \left( s ^ { \prime ( t ) } , j \right) < \theta ( s , i ) \right\} - \tau _ { i } \right] .
$$

By construction, E $\left[ H ^ { ( t ) } ( \pmb { \theta } ) \right] = D _ { \mu } h ( \pmb { \theta } )$ . Consequently, the asynchronous QTD update can be written as

$$
\begin{array} { r l } & { \pmb \theta ^ { ( t ) } = \pmb \theta ^ { ( t - 1 ) } - \alpha _ { t - 1 } \left[ D _ { \mu } \pmb h ( \pmb \theta ^ { ( t - 1 ) } ) + \pmb H ^ { ( t ) } ( \pmb \theta ^ { ( t - 1 ) } ) - D _ { \mu } \pmb h ( \pmb \theta ^ { ( t - 1 ) } ) \right] } \\ & { \qquad : = \pmb \theta ^ { ( t - 1 ) } - \alpha _ { t - 1 } \left[ D _ { \mu } \pmb h ( \pmb \theta ^ { ( t - 1 ) } ) + \pmb \epsilon _ { t - 1 } \right] . } \end{array}
$$

Since $\mu ( s ) > 0$ for every $s \in { \mathcal { S } } , D _ { \mu }$ is invertible. Hence $\scriptstyle D _ { \mu } h$ has the same unique zero $\pmb { \theta } _ { m }$ as $^ { h }$ Moreover,

$$
\nabla \left( D _ { \mu } h \right) \left( \theta _ { m } \right) = D _ { \mu } \nabla h ( \theta _ { m } ) = D _ { \mu } G .
$$

By Lemma 4.3, $\pmb { { \cal D } } _ { \mu } \pmb { G }$ is uniformly repulsive. Furthermore, since ∇h is Lipschitz continuous in a neighborhood of $\pmb { \theta } _ { m }$ and $D _ { \mu }$ is fixed, $\nabla ( D _ { \mu } h )$ is also Lipschitz continuous in a neighborhood of $\pmb { \theta } _ { m }$ Therefore,

$$
\| D _ { \mu } h ( \theta ) - D _ { \mu } G ( \theta - \theta _ { m } ) \| \leqslant L \| \theta - \theta _ { m } \| ^ { 2 }
$$

for all θ in a suficiently small neighborhood of $\pmb { \theta } _ { m }$ , and hence Equation (9) holds.

It remains to verify the noise conditions. Recall that

$$
\epsilon _ { t - 1 } = { \cal H } ^ { ( t ) } ( \pmb { \theta } ^ { ( t - 1 ) } ) - D _ { \mu } \pmb { h } ( \pmb { \theta } ^ { ( t - 1 ) } ) .
$$

Then $\epsilon _ { t }$ is $\mathcal { F } _ { t }$ -measurable. Since the sample used at iteration t is independent of $\mathcal { F } _ { t - 2 }$ , conditional on the past we have $\mathbb { E } [ \epsilon _ { t } \mid \mathcal { F } _ { t - 1 } ] = 0$ . Again, each coordinate of ${ \pmb H } ^ { ( t ) } ( { \pmb \theta } )$ is uniformly bounded, so $( \epsilon _ { t } ) _ { t \geq 0 }$ is uniformly bounded almost surely. Furthermore, by the almost sure convergence $\pmb { \theta } ^ { ( t ) }  \pmb { \theta } _ { m }$ and the continuity of the corresponding conditional covariance matrix, $\mathbb { E } [ \mathbf { \epsilon } _ { t } \mathbf { \epsilon } _ { t } ^ { \top } \mid \mathcal { F } _ { t - 1 } ]  \mathbf { \Gamma } _ { \mathrm { a s y n } }$ almost surely. Thus all the conditions of Theorem 4.1 are satisfied with

$$
M = D _ { \mu } G , \Gamma = \Gamma _ { \mathrm { a s y n } } .
$$

Applying Theorem 4.1 completes the proof.

## 5 Conclusions

In this paper, we studied how to perform statistical inference for QTD in distributional reinforcement learning. We established functional central limit theorems for the averaged iterates of QTD in both synchronous and asynchronous settings, characterizing their weak convergence to a rescaled Brownian motion and providing explicit expressions for the covariance matrices. Based on these results, we developed online inference procedures using random scaling, which construct an asymptotically pivotal statistic without requiring explicit estimation of the asymptotic covariance. Moreover, we showed that the proposed inference procedures can be implemented online by recursively updating the required quantities along the QTD trajectory, avoiding the need to store the entire sequence of iterates.

There are several interesting issues for future work. One future direction is to generalize our results to more general step size schedules. Another promising direction for future work is to go beyond the asymptotic framework and establish non-asymptotic convergence rates for QTD. Such results would provide a more complete understanding of the finite-sample behavior of QTD and may further facilitate statistical estimation and inference in reinforcement learning.

## References

[1] M. G. Bellemare, W. Dabney, and R. Munos. A distributional perspective on reinforcement learning. In International conference on machine learning, pages 449–458. PMLR, 2017.

[2] M. G. Bellemare, S. Candido, P. S. Castro, J. Gong, M. C. Machado, S. Moitra, S. S. Ponda, and Z. Wang. Autonomous navigation of stratospheric balloons using reinforcement learning. Nature, 588(7836):77–82, 2020.

[3] M. G. Bellemare, W. Dabney, and M. Rowland. Distributional Reinforcement Learning. MIT Press, 2023. http://www.distributional-rl.org.

[4] C. Bodnar, A. Li, K. Hausman, P. Pastor, and M. Kalakrishnan. Quantile qt-opt for risk-aware vision-based robotic grasping. arXiv preprint arXiv:1910.02787, 2019.

[5] Y. Chandak, S. Niekum, B. da Silva, E. Learned-Miller, E. Brunskill, and P. S. Thomas. Universal of-policy evaluation. Advances in Neural Information Processing Systems, 34:27475–27490, 2021.

[6] Z. Cheng, Y. Peng, and Z. Zhang. Statistical eficiency and inference of quantile distributional reinforcement learning. arXiv preprint arXiv:2607.08444, 2026.

[7] W. Dabney, G. Ostrovski, D. Silver, and R. Munos. Implicit quantile networks for distributional reinforcement learning. In International conference on machine learning, pages 1096–1105. PMLR, 2018.

[8] W. Dabney, M. Rowland, M. Bellemare, and R. Munos. Distributional reinforcement learning with quantile regression. In Proceedings of the AAAI Conference on Artificial Intelligence, 2018.

[9] T. Doan, B. Mazoure, and C. Lyle. Gan q-learning. arXiv preprint arXiv:1805.04874, 2018.

[10] D. Freirich, T. Shimkin, R. Meir, and A. Tamar. Distributional multivariate policy evaluation and exploration with the bellman gan. In International Conference on Machine Learning, pages 1983–1992. PMLR, 2019.

[11] B. Hao, X. Ji, Y. Duan, H. Lu, C. Szepesvari, and M. Wang. Bootstrapping fitted q-evaluation for of-policy inference. In International Conference on Machine Learning, pages 4074–4084. PMLR, 2021.

[12] A. Huang, L. Leqi, Z. Lipton, and K. Azizzadenesheli. Of-policy risk assessment for markov decision processes. In International Conference on Artificial Intelligence and Statistics, pages 5022–5050. PMLR, 2022.

[13] N. Jiang and L. Li. Doubly robust of-policy value evaluation for reinforcement learning. In International Conference on Machine Learning, pages 652–661. PMLR, 2016.

[14] X. Li, J. Liang, X. Chang, and Z. Zhang. Statistical estimation and inference via local sgd in federated learning. arXiv preprint arXiv:2109.01326, 2021.

[15] X. Li, J. Liang, and Z. Zhang. Online statistical inference for nonlinear stochastic approximation with markovian data. arXiv preprint arXiv:2302.07690, 2023.

[16] X. Li, W. Yang, J. Liang, Z. Zhang, and M. I. Jordan. A statistical analysis of polyak-ruppert averaged q-learning. In International Conference on Artificial Intelligence and Statistics, pages 2207–2261. PMLR, 2023.

[17] T. Morimura, M. Sugiyama, H. Kashima, H. Hachiya, and T. Tanaka. Nonparametric return distribution approximation for reinforcement learning. In Proceedings of the 27th International Conference on Machine Learning (ICML-10), pages 799–806, 2010.

[18] Y. Peng, L. Zhang, and Z. Zhang. Statistical eficiency of distributional temporal diference learning. Advances in Neural Information Processing Systems, 37:24724–24761, 2024.

[19] Y. Peng, K. Jin, L. Zhang, and Z. Zhang. A finite sample analysis of distributional td learning with linear function approximation. arXiv preprint arXiv:2502.14172, 2025.

[20] B. T. Polyak and A. B. Juditsky. Acceleration of stochastic approximation by averaging. SIAM journal on control and optimization, 30(4):838–855, 1992.

[21] Z. Qi, C. Bai, Z. Wang, and L. Wang. Distributional of-policy evaluation in reinforcement learning. Journal of the American Statistical Association, 120(551):1517–1530, 2025.

[22] M. Rowland, M. Bellemare, W. Dabney, R. Munos, and Y. W. Teh. An analysis of categorical distributional reinforcement learning. In International Conference on Artificial Intelligence and Statistics, pages 29–37. PMLR, 2018.

[23] M. Rowland, R. Munos, M. G. Azar, Y. Tang, G. Ostrovski, A. Harutyunyan, K. Tuyls, M. G. Bellemare, and W. Dabney. An analysis of quantile temporal-diference learning. arXiv preprint arXiv:2301.04462, 2023.

[24] M. Rowland, Y. Tang, C. Lyle, R. Munos, M. G. Bellemare, and W. Dabney. The statistical benefits of quantile temporal-diference learning for value estimation. In International Conference on Machine Learning, pages 29210–29231. PMLR, 2023.

[25] C. Shi, S. Zhang, W. Lu, and R. Song. Statistical inference of the value function for reinforcement learning in infinite-horizon settings. Journal of the Royal Statistical Society Series B: Statistical Methodology, 84(3):765–793, 2022.

[26] P. Thomas, G. Theocharous, and M. Ghavamzadeh. High-confidence of-policy evaluation. In Proceedings of the AAAI Conference on Artificial Intelligence, 2015.

[27] D. Yang, L. Zhao, Z. Lin, T. Qin, J. Bian, and T.-Y. Liu. Fully parameterized quantile function for distributional reinforcement learning. Advances in neural information processing systems, 32, 2019.

[28] W. Yang, L. Zhang, and Z. Zhang. Toward theoretical understandings of robust markov decision processes: Sample complexity and asymptotics. The Annals of Statistics, 50(6):3223–3248, 2022.

[29] L. Zhang, Y. Peng, J. Liang, W. Yang, and Z. Zhang. Estimation and inference in distributional reinforcement learning. The Annals of Statistics, 53(5):1987–2011, 2025.

[30] Y. Zhu, J. Dong, and H. Lam. Uncertainty quantification and exploration for reinforcement learning. Operations Research, 2023.

## A Omitted Proofs in Section 3

Proof. We only present the argument for synchronous QTD, since the asynchronous case follows identically. Define

$$
\Phi _ { T } ( u ) : = \frac { 1 } { \sqrt T } \sum _ { t = 1 } ^ { \lfloor T u \rfloor } \left( \pmb { \theta } ^ { ( t ) } - \pmb { \theta } _ { m } \right) , \qquad u \in [ 0 , 1 ] .
$$

Then

$$
\sqrt { T } \left( \bar { \pmb { \theta } } _ { T } - \pmb { \theta } _ { m } \right) = \pmb { \Phi } _ { T } ( 1 ) ,
$$

$$
\frac { 1 } { \sqrt { T } } \left\{ \sum _ { k = 1 } ^ { t } \pmb { \theta } ^ { ( k ) } - t \bar { \pmb { \theta } } _ { T } \right\} = \Phi _ { T } ( t / T ) - \frac { t } { T } \Phi _ { T } ( 1 ) .
$$

It follows from Theorem 3.1 and the continuous mapping theorem that

$$
\begin{array} { l } { \displaystyle T \hat { V } _ { T } = \frac { 1 } { T } \sum _ { t = 1 } ^ { T } \left\{ \Phi _ { T } ( t / T ) - \frac { t } { T } \Phi _ { T } ( 1 ) \right\} \left\{ \Phi _ { T } ( t / T ) - \frac { t } { T } \Phi _ { T } ( 1 ) \right\} ^ { \top } } \\ { \displaystyle \Rightarrow G ^ { - 1 } \Gamma _ { \mathrm { s y n } } ^ { 1 / 2 } \left[ \int _ { 0 } ^ { 1 } \left( B ( u ) - u B ( 1 ) \right) \left( B ( u ) - u B ( 1 ) \right) ^ { \top } \mathrm { d } u \right] \left( G ^ { - 1 } \Gamma _ { \mathrm { s y n } } ^ { 1 / 2 } \right) ^ { \top } . } \end{array}
$$

Let $\sigma _ { c } ^ { 2 } : = c ^ { \top } \Sigma _ { \mathrm { s y n } } c .$ then $\sigma _ { c } ^ { - 1 } c ^ { \top } G ^ { - 1 } { \bf { r } } _ { \mathrm { { s y n } } } ^ { 1 / 2 } B ( \cdot )$ is a one-dimensional standard Brownian motion. Therefore, another application of the continuous mapping theorem yields

$$
\frac { c ^ { \top } \left( \bar { \theta } _ { T } - \theta _ { m } \right) } { \sqrt { c ^ { \top } \hat { V } _ { T } c } } \Rightarrow \frac { B ( 1 ) } { \left[ \int _ { 0 } ^ { 1 } \left( B ( u ) - u B ( 1 ) \right) ^ { 2 } \mathrm { d } u \right] ^ { 1 / 2 } } .
$$

This completes the proof.

## B Omitted Proofs in Section 4

Proof of Theorem $4 . 1 .$ . Denote $\Delta _ { t } = Z _ { t } - z ^ { * }$ and $\pmb { \rho } _ { t } = h ( \pmb { Z } _ { t } ) - M ( \pmb { Z } _ { t } - \pmb { z } ^ { \ast } )$ , then

$$
\begin{array} { r } { \pmb { \Delta } _ { t } = ( \pmb { I } - \alpha _ { t - 1 } \pmb { M } ) \pmb { \Delta } _ { t - 1 } - \alpha _ { t - 1 } \big ( \epsilon _ { t - 1 } + \pmb { \rho } _ { t - 1 } \big ) . } \end{array}
$$

Therefore,

$$
\pmb { \Delta } _ { t } = \prod _ { i = 0 } ^ { t - 1 } ( \pmb { I } - \alpha _ { i } \pmb { M } ) \pmb { \Delta } _ { 0 } - \sum _ { i = 0 } ^ { t - 1 } \alpha _ { i } \prod _ { l = i + 1 } ^ { t - 1 } ( \pmb { I } - \alpha _ { l } \pmb { M } ) ( \epsilon _ { i } + \pmb { \rho } _ { i } ) .
$$

Define

$$
A _ { i } ^ { t } = \alpha _ { i } \sum _ { k = i } ^ { t - 1 } \prod _ { l = i } ^ { j - 1 } ( I - \alpha _ { l } { \cal M } ) ,
$$

then

$$
\begin{array} { r l } { \displaystyle \frac { 1 } { \sqrt { T } } \displaystyle \sum _ { i = 1 } ^ { \lfloor \lfloor \frac { m + 1 } { 2 } \rfloor } \Delta _ { i } , - T ^ { - \frac { 1 } { 2 } } \alpha _ { 0 } ^ { - 1 } A _ { i } ^ { \lfloor \frac { m + 1 } { 2 } \rfloor + 1 } \Delta _ { 0 } } & { } \\ { - T ^ { - \frac { 1 } { 2 } } \displaystyle \sum _ { i = 0 } ^ { \lfloor \frac { m + 1 } { 2 } \rfloor } \frac { \alpha _ { i } } { \alpha _ { i + 1 } } A _ { i + 1 } ^ { \lfloor \frac { m + 1 } { 2 } \rfloor + 1 } { \rho _ { i } } } & { } \\ { - T ^ { - \frac { 1 } { 2 } } \displaystyle \sum _ { i = 0 } ^ { \lfloor \frac { m } { 2 } \rfloor } \frac { \alpha _ { i } } { \alpha _ { i + 1 } } \left( A _ { i + 1 } ^ { \lfloor \frac { m + 1 } { 2 } \rfloor + 1 } - A _ { i + 1 } ^ { ( \frac { m + 1 } { 2 } ) } \right) \epsilon } & { } \\ { - T ^ { - \frac { 1 } { 2 } } \displaystyle \sum _ { i = 0 } ^ { \lfloor \frac { m } { 2 } \rfloor } \left( \frac { \alpha _ { i } } { \alpha _ { i + 1 } } A _ { i + 1 } ^ { \lfloor \frac { m + 1 } { 2 } \rfloor + 1 } - G ^ { - i } \right) \epsilon } & { } \\ { - T ^ { - \frac { 1 } { 2 } } \displaystyle \sum _ { i = 0 } ^ { \lfloor \frac { m } { 2 } \rfloor } G ^ { - 1 } \epsilon } & { } \\ { - T ^ { - \frac { 1 } { 2 } } \displaystyle \sum _ { i = 0 } ^ { \lfloor \frac { m } { 2 } \rfloor } \epsilon + T _ { i } + \int _ { i } } & { } \\ { s - T _ { 0 } + T _ { 1 } + J _ { 2 } + T _ { 4 } + J _ { 4 } } & { } \end{array}
$$

By Lemma 1 in Polyak and Juditsky [20], there exists a constant C such that $\| A _ { i } ^ { t } \| \leqslant C$ for all $t , i$

$$
\operatorname* { s u p } _ { u \in [ 0 , 1 ] } \| I _ { 0 } \| \leqslant T ^ { - \frac { 1 } { 2 } } C \alpha _ { 0 } ^ { - 1 } \| \pmb { \Delta } _ { 0 } \|  0
$$

almost surely. For $I _ { 2 } .$ , we know that

$$
\operatorname* { s u p } _ { u \in [ 0 , 1 ] } \| I _ { 1 } \| \lesssim T ^ { - \frac { 1 } { 2 } } \sum _ { i = 0 } ^ { T } \| \pmb { \rho } _ { i } \| \to 0
$$

almost surely by Lemma B.1. For $I _ { 3 }$ , we have

$$
\begin{array} { r l } & { \left\| T ^ { - \frac { 1 } { 2 } } \sum _ { i = 0 } ^ { \lfloor { T M } \rfloor } \frac { \alpha _ { i } } { \alpha _ { i + 1 } } \left( A _ { i + 1 } ^ { [ { T u } ] + 1 } - A _ { i + 1 } ^ { { T + 1 } } \right) \epsilon _ { i } \right\| } \\ & { = \left\| T ^ { - \frac { 1 } { 2 } } \sum _ { i = 0 } ^ { \lfloor { T M } \rfloor } \alpha _ { i } \sum _ { { \substack { \lfloor T M \rfloor + 1 } \lfloor { { l - { \theta } } } \rfloor } } ^ { T } \prod _ { i } ^ { { \theta } - 1 } ( I - { \alpha _ { l } } G ) \epsilon _ { i } \right\| } \\ & { = \left\| A _ { [ { T u } ] + 1 } ^ { { T + 1 } } \alpha _ { [ { T u } ] + 1 } ^ { - 1 } \sum _ { i = 0 } ^ { \lfloor { T u } \rfloor } \sum _ { { l } = i + 1 } ^ { \lfloor { T u } \rfloor } ( I - { \alpha _ { l } } G ) \epsilon _ { i } \right\| } \end{array}
$$

$$
\lesssim \alpha _ { \lfloor T u \rfloor + 1 } ^ { - 1 } \left\| \sum _ { i = 0 } ^ { \lfloor T u \rfloor } \alpha _ { i } \prod _ { \substack { l = i + 1 } } ^ { \lfloor T u \rfloor } ( I - \alpha _ { l } \pmb { G } ) \epsilon _ { i } \right\| .
$$

By Lemma 4 in Li et al. [15],

$$
\operatorname* { s u p } _ { u \in [ 0 , 1 ] } \| I _ { 2 } \| \lesssim \operatorname* { s u p } _ { u \in [ 0 , 1 ] } T ^ { - \frac { 1 } { 2 } } \alpha _ { \lfloor T u \rfloor + 1 } ^ { - 1 } \left\| \sum _ { i = 0 } ^ { \lfloor T u \rfloor } \alpha _ { i } \prod _ { l = i + 1 } ^ { \lfloor T u \rfloor } ( I - \alpha _ { l } G ) \epsilon _ { i } \right\| \to 0
$$

in probability. Moreover, by Lemma 1 in Polyak and Juditsky [20],

$$
\operatorname* { l i m } _ { T \to \infty } \frac { 1 } { T } \sum _ { t = 0 } ^ { T } \Big \| \pmb { A } _ { i + 1 } ^ { T + 1 } - \pmb { G } ^ { - 1 } \Big \| = 0 .
$$

Therefore, by Doob’s maximal inequality,

$$
\begin{array} { r l } & { \mathbb { E } \left[ \displaystyle \operatorname* { s u p } _ { [ s = 0 , 1 ] } \big | I _ { 1 , 0 } \right] ^ { 2 } } \\ & { \leqslant \mathbb { E } \Big \| T \frac { 1 } { \lambda _ { \mathrm { t o t } } ^ { 2 } } \sum _ { i = 0 } ^ { i } \left( \frac { \Omega _ { i } } { \alpha _ { i + 1 } } A _ { i + 1 } ^ { \mathcal { T } _ { 1 } } - G \mathrm { \nabla } ^ { i } \right) \epsilon _ { i } \Big \| ^ { 2 } } \\ & { = { T ^ { - 1 } } \displaystyle \sum _ { i = 0 } ^ { T } \mathbb { E } \bigg | \left( \frac { \alpha _ { i } } { \alpha _ { i + 1 } } A _ { i + 1 } ^ { \mathcal { T } _ { 1 } } - G \mathrm { \nabla } ^ { - i } \right) \epsilon _ { i } \bigg | ^ { 2 } } \\ & { \leqslant { T ^ { - 1 } } \displaystyle \sum _ { i = 0 } ^ { T } \left[ \Big \| A _ { i + 1 } ^ { \mathcal { T } _ { 1 } } - G \mathrm { \nabla } ^ { - 1 } \Big \| ^ { 2 } + \left( \frac { \alpha _ { i } } { \alpha _ { i + 1 } } - 1 \right) ^ { 2 } \right] } \\ & { \leqslant { T ^ { - 1 } } \displaystyle \sum _ { i = 0 } ^ { T } \left[ \Big | A _ { i + 1 } ^ { \mathcal { T } _ { 1 } } - G \mathrm { \nabla } ^ { - 1 } \Big | + ( i + 1 ) ^ { - 2 } \right] \to 0 . } \end{array}
$$

Finally, by Lemma A.3 in Li et al. [14], $I _ { 4 } \Rightarrow G ^ { - 1 } \Gamma _ { \mathrm { s y n } } ^ { \frac { 1 } { 2 } } B ( \cdot )$ and the proof is completed.

The following lemma is proved in Appendix C.

Lemma B.1. With probability 1, we have

$$
\sum _ { i = 0 } ^ { \infty } ( i + 1 ) ^ { - \frac { 1 } { 2 } } \left\| \pmb { \rho } _ { i } \right\| < \infty .
$$

Proof of Lemma 4.1. By Lemma A.2 in Cheng et al. [6], $p ( \mathcal { T } ^ { \pi } \pmb { \eta } _ { m } ) ( s ) \big ( \theta _ { m } ( s , i ) \big ) > 0$ for every ps, iq and

$p ( \tau ^ { \pi } \eta _ { m } ) ( s )$ is continuous at every $\theta _ { m } ( s , i )$ . Therefore, for every $\pmb { \lambda } \in [ 0 , 1 ] ^ { s \times [ m ] }$

$$
\Pi _ { m } ^ { \lambda } \mathcal { T } ^ { \pi } \pmb { \eta } _ { m } = \Pi _ { m } \mathcal { T } ^ { \pi } \pmb { \eta } _ { m } = \pmb { \eta } _ { m } .
$$

Therefore Lemma 4.1 holds.

Proof of Lemma 4.2. Define $\pmb { \Lambda } = \left\{ \pmb { \theta } _ { m } ^ { \lambda } : \pmb { \eta } _ { \pmb { \theta } _ { m } ^ { \lambda } } = \pmb { \Pi } _ { m } ^ { \lambda } \mathcal { T } ^ { \pi } \pmb { \eta } _ { \pmb { \theta } _ { m } ^ { \lambda } } , \lambda \in [ 0 , 1 ] ^ { \mathcal { S } \times [ m ] } \right\}$ . By Theorem 8 in Rowland et al. [23], with probability 1,

$$
\operatorname* { l i m } _ { t \to \infty } \operatorname* { i n f } _ { \pmb { \theta } ^ { \ast } \in \pmb { \Lambda } } \Big \| \pmb { \theta } ^ { ( t ) } - \pmb { \theta } ^ { \ast } \Big \| = 0 .
$$

However, Lemma 4.1 implies that $\Lambda = \{ \pmb { \theta } _ { m } \}$ and the conclusion follows.

Proof of Lemma 4.3. By Gershgorin circle theorem, if λ is an eigenvalue of $G ,$

$$
\begin{array} { c l c r } { { } } & { { } } & { { R e ( \lambda ) \geqslant \displaystyle \operatorname* { m i n } _ { ( s , i ) } \left\{ G _ { ( s , i ) , ( s , i ) } - \sum _ { ( s ^ { \prime } , j ) \neq ( s , i ) } | G _ { ( s , i ) , ( s ^ { \prime } , j ) } | \right\} } } \\ { { } } & { { } } & { { = \displaystyle \operatorname* { m i n } _ { ( s , i ) } \left\{ p _ { ( \mathcal { T } ^ { \pi } \eta _ { m } ) ( s ) } ( \theta _ { m } ( s , i ) ) \right\} > 0 , } } \end{array}
$$

where the last inequality follows from Lemma A.2 in Cheng et al. [6]. Similarly, if λ is an eigenvalue of $\pmb { { \cal D } } _ { \mu } \pmb { G }$

$$
\begin{array} { l } { { \displaystyle R e ( \lambda ) \geqslant \operatorname* { m i n } _ { ( s , i ) } \left\{ ( D _ { \mu } G ) _ { ( s , i ) , ( s , i ) } - \sum _ { ( s ^ { \prime } , j ) \neq ( s , i ) } \big \vert ( D _ { \mu } G ) _ { ( s , i ) , ( s ^ { \prime } , j ) } \big \vert \right\} } } \\ { { \displaystyle \ = \operatorname* { m i n } _ { ( s , i ) } \left\{ \mu ( s ) p _ { ( { \mathcal T } ^ { \pi } \eta _ { m } ) ( s ) } ( \theta _ { m } ( s , i ) ) \right\} > 0 } } \end{array}
$$

and the proof is completed.

## C Technical Lemmas

Proof of Lemma B.1. We construct a Lyapunov function first. Since M is uniform repulsive, there exists a positive definite metrix $Q$ satisfying $M Q + Q M ^ { \top } = I$ . In fact, the solution is given by

$$
Q = \int _ { 0 } ^ { \infty } e ^ { - M t } e ^ { M ^ { \top } t } \mathrm { d } t .
$$

Let $0 < p _ { - } \leqslant p _ { + } < \infty$ be the smallest and largest eigenvalues of $Q .$ . Then

$$
p _ { - } \left\| z \right\| ^ { 2 } \leqslant V ( z ) : = z ^ { \top } Q z \leqslant p _ { + } \left\| z \right\| ^ { 2 } .\tag{10}
$$

Choose κ such that $0 < \kappa \leqslant$ min $\left\{ \delta , ( 4 p _ { + } L ) ^ { - 1 } \right\}$ . Then, for $\begin{array} { r l r } {  { \| z - z ^ { * } \| \leqslant \kappa . } } \end{array}$ denote ${ \bf { \nabla } } _ { ^ { \bf { \lambda } } { \bf { \mathcal { X } } } } =$

$$
\begin{array} { r l } & { \quad 2 ( z - z ^ { * } ) ^ { \top } Q h ( z ) } \\ & { = 2 ( z - z ^ { * } ) ^ { \top } Q M ( z - z ^ { * } ) + 2 ( z - z ^ { * } ) ^ { \top } Q \left[ h ( z ) - M ( z - z ^ { * } ) \right] } \\ & { = ( z - z ^ { * } ) ^ { \top } \left( Q M + M ^ { \top } Q \right) ( z - z ^ { * } ) + 2 ( z - z ^ { * } ) ^ { \top } Q \left[ h ( z ) - M ( z - z ^ { * } ) \right] } \\ & { = \left\| z - z ^ { * } \right\| ^ { 2 } + 2 ( z - z ^ { * } ) ^ { \top } Q \left[ h ( z ) - M ( z - z ^ { * } ) \right] } \\ & { \geqslant \left\| z - z ^ { * } \right\| ^ { 2 } - 2 p _ { + } K _ { h } \left\| z - z ^ { * } \right\| ^ { 3 } } \\ & { \geqslant \frac { 1 } { 2 } \left\| z - z ^ { * } \right\| ^ { 2 } , } \end{array}
$$

Also, for $\| z - z ^ { * } \| \leqslant \kappa$

$$
\begin{array} { l } { \displaystyle \| \pmb { h } ( z ) \| \leqslant \| \pmb { M } ( z - z ^ { * } ) \| + \| \pmb { h } ( z ) - \pmb { M } ( z - z ^ { * } ) \| } \\ { \leqslant \left( \| \pmb { M } \| + K _ { h } \delta \right) \| z - z ^ { * } \| . } \end{array}
$$

Denote $H = \lVert M \rVert + K _ { h } \delta$ and

$$
\begin{array} { l } { { \displaystyle V \left( z - z ^ { * } - \gamma h ( z ) \right) - V \left( z - z ^ { * } \right) } } \\ { { \displaystyle = - 2 \gamma ( z - z ^ { * } ) ^ { \top } Q h ( z ) + \gamma ^ { 2 } h ( z ) ^ { \top } Q h ( z ) } } \\ { { \displaystyle \leqslant - \frac { \gamma } { 2 } \left\| z - z ^ { * } \right\| ^ { 2 } + \gamma ^ { 2 } p _ { + } H ^ { 2 } \left\| z - z ^ { * } \right\| ^ { 2 } } . } \end{array}
$$

Since $\alpha _ { t } \ \to \ 0$ , there exists $t _ { 0 }$ such that $\alpha _ { t } p _ { + } H ^ { 2 } \ \leqslant \ \frac { 1 } { 4 }$ and $c \alpha _ { t } \ \leqslant \ 1$ for all $t \ \geqslant \ t _ { 0 }$ , where $c : = ( 4 p _ { + } ) ^ { - 1 } > 0$ . Thus, for every $t \geqslant t _ { 0 }$ and every $\| z - z ^ { * } \| \leqslant \kappa .$

$$
\begin{array} { c } { { V ( z - z ^ { * } - \gamma _ { t } \pmb { h } ( z ) ) \leqslant V ( z - z ^ { * } ) - \displaystyle \frac { \alpha _ { t } } { 4 } \| z - z ^ { * } \| ^ { 2 } } } \\ { { \leqslant ( 1 - c \alpha _ { t } ) V ( z - z ^ { * } ) . } } \end{array}
$$

Now we prove the desired conclusion. Fix integer $T \geqslant t _ { 0 }$ and define the stopping time

$$
\tau _ { T } : = \operatorname* { i n f } \left\{ t \geqslant T : \left\| Z _ { t } - z ^ { * } \right\| > \delta \right\} ,\tag{11}
$$

with inf $\mathcal { O } = \infty$ , and the event $G _ { T } : = \{ \tau _ { T } = \infty \}$

We have

$$
\begin{array} { r l } & { \mathbb { E } [ V ( \pmb { Z } _ { t } - z ^ { * } ) \mathbb { 1 } \{ \tau _ { T } \geqslant t \} \ : | \mathcal { F } _ { t - 1 } ] } \\ & { \leqslant \mathbb { 1 } \big \{ \tau _ { T } \geqslant t - 1 \big \} \mathbb { E } [ V ( \pmb { Z } _ { t } - z ^ { * } ) \ : | \mathcal { F } _ { t - 1 } ] } \\ & { = \mathbb { 1 } \big \{ \tau _ { T } \geqslant t - 1 \big \} [ V ( \pmb { Z } _ { t - 1 } - z ^ { * } ) + \alpha _ { t - 1 } ^ { 2 } \mathbb { E } [ \epsilon _ { t } ^ { \top } \pmb { Q } \epsilon _ { t } \ : | \ : \mathcal { F } _ { t - 1 } ]  } \\ & {  \leqslant ( 1 - c \alpha _ { t - 1 } ) V ( \pmb { Z } _ { t - 1 } - z ^ { * } ) \mathbb { 1 } \{ \tau _ { T } \geqslant t - 1 \} + C \alpha _ { t - 1 } ^ { 2 } . } \end{array}
$$

For $t \geqslant T + 1$ , define $u _ { t } : = \mathbb { E } [ V ( Z _ { t } - z ^ { * } ) \mathbb { 1 } \{ \tau _ { T } \geqslant t \} ]$ , then

$$
u _ { t } \leqslant \big ( 1 - c \alpha _ { t - 1 } \big ) u _ { t - 1 } + C _ { \epsilon } \alpha _ { t - 1 } ^ { 2 } .
$$

By Lemma A.12 in Li et al. [14], for $t \geqslant T , u _ { t } \leqslant C _ { T } \alpha _ { t }$

Since $G _ { T } \subseteq \{ \tau _ { T } \geqslant t \}$ , we know that for $t \geqslant T .$

$$
\begin{array} { r } { \mathbb { E } \| \pmb { \rho } _ { t } \| \mathbb { 1 } _ { G _ { T } } \lesssim \mathbb { E } \| Z _ { t } - z ^ { * } \| ^ { 2 } \mathbb { 1 } _ { G _ { T } } \leqslant C _ { T } ^ { \prime } \alpha _ { t } . } \end{array}
$$

Therefore, we know that

$$
\mathbb { E } \sum _ { t = T } ^ { \infty } \frac { \| \rho _ { t } \| \mathbb { 1 } _ { G _ { T } } } { \sqrt { t + 1 } } \lesssim \sum _ { t = T } ^ { \infty } ( t + 1 ) ^ { - ( a + \frac { 1 } { 2 } ) } < \infty .
$$

Hence on $G _ { T }$ , we know that

$$
\sum _ { t = T } ^ { \infty } { \frac { \left\| \rho _ { t } \right\| } { \sqrt { t + 1 } } } < \infty
$$

and the conclusion holds. However, since $Z _ { t } \to z ^ { * }$ almost surely,

$$
\mathbb { P } \left( \bigcup _ { T = 1 } ^ { \infty } G _ { T } \right) = 1 .
$$

Therefore, we know that with probability 1,

$$
\sum _ { t = 0 } ^ { \infty } \frac { \| \pmb { \rho } _ { t } \| } { \sqrt { t + 1 } } < \infty .
$$